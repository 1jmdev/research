# Efficient Pre-Training with Token Superposition

**arXiv:** [2605.06546](https://arxiv.org/abs/2605.06546) · [PDF](https://arxiv.org/pdf/2605.06546) · [HTML](https://arxiv.org/html/2605.06546) · **First submitted:** 2026-05-19  
**Category:** [Pre-training recipes & efficiency](README.md) — **rank #17 of 135** by impact score  

> **TL;DR** — Token-Superposition Training (TST), a simple drop-in method that significantly improves the data throughput per FLOPs during pre-training without modifying the parallelism, optimizer, tokenizer, data, or model architecture, is presented.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Bowen Peng, Théo Gigant, Jeffrey Quesnelle |
| Organisation (HF) | NousResearch |
| arXiv categories | cs.CL |
| arXiv comments | 25 pages, 11 figures, 28 tables |
| Citations (Semantic Scholar, 2026-09-25) | 1 (0 influential) |
| Hugging Face upvotes | 46 |
| Code | — |
| Training requirement (from abstract) | training-free / post-training; involves (re)training / fine-tuning |
| Impact score | 4.88 |

## Key results (claimed in abstract)

- Ultimately, TST consistently outperforms baseline loss and downstream evaluations, and under equal-loss settings, TST yields up to a 2.5x reduction in total pre-training time at the 10B A1B scale.

## Abstract

Pre-training of Large Language Models is often prohibitively expensive and inefficient at scale, requiring complex and invasive modifications in order to achieve high data throughput. In this work, we present Token-Superposition Training (TST), a simple drop-in method that significantly improves the data throughput per FLOPs during pre-training without modifying the parallelism, optimizer, tokenizer, data, or model architecture. TST is done in two phases: (i) A highly efficient superposition phase where we combine many contiguous tokens into one bag and train using a multi-hot cross-entropy (MCE) objective, and (ii) a recovery phase where we revert back to standard training. We extensively evaluate TST on the scale of 270M and 600M parameters and validate on 3B and a 10B A1B mixture of experts model, demonstrating that it is highly robust in different settings. Ultimately, TST consistently outperforms baseline loss and downstream evaluations, and under equal-loss settings, TST yields up to a 2.5x reduction in total pre-training time at the 10B A1B scale.

**Keywords:** Token-Superposition Training, multi-hot cross-entropy, pre-training, FLOPs, data throughput, parallelism, optimizer, tokenizer, model architecture, superposition phase, recovery phase

## Reproduce / read

```sh
arxiv read 2605.06546            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2605.06546 "method"
arxiv bibtex 2605.06546
```
