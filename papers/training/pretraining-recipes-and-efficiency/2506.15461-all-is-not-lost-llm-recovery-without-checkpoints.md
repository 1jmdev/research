# All is Not Lost: LLM Recovery without Checkpoints

**arXiv:** [2506.15461](https://arxiv.org/abs/2506.15461) · [PDF](https://arxiv.org/pdf/2506.15461) · [HTML](https://arxiv.org/html/2506.15461) · **First submitted:** 2026-04-04  
**Category:** [Pre-training recipes & efficiency](README.md) — **rank #16 of 143** by impact score  

> **TL;DR** — This paper proposes, CheckFree, an efficient recovery method where a failing stage (in a pipeline) is substituted by weighted averaging of the closest neighboring stages, which requires no additional computation or storage.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Nikolay Blagoev, Oğuzhan Ersoy, Lydia Yiyu Chen |
| Organisation (HF) | Gensyn |
| arXiv categories | cs.DC, cs.LG |
| Venue | EuroMLSys@EuroSys |
| Citations (Semantic Scholar, 2026-09-25) | 3 (0 influential) |
| Hugging Face upvotes | 41 |
| Code | https://github.com/gensyn-ai/CheckFree |
| Models used | LLaMa |
| Hardware | H100 (×3 mentions) |
| **Compute (est. H100-hours)** | **0.22** (auto-extracted, see below) |
| Largest token count mentioned | 10M tokens |
| Impact score | 6.71 |

## Key results (claimed in abstract)

- In the case of low and medium failure rates (5-10%), CheckFree and CheckFree+ outperform both checkpointing and redundant computation in terms of convergence wall-clock time, achieving up to 12% improvement over redundant computation.

## Abstract

Training LLMs on decentralized nodes or on-spot instances, lowers the training cost and enables model democratization. The inevitable challenge here is the transient churns of nodes due to failures and the operator's scheduling policies, leading to losing parts of the model (some layers). The conventional approaches to recover from failures is to either use checkpointing, where periodically a copy of the entire model is sent to an additional storage, or redundant computation. These approaches yield significant communication and/or computation overhead even in non-failure cases and scale poorly in settings with large models. In this paper we propose CheckFree, an efficient recovery method where a failing stage is substituted by weighted averaging of the closest neighboring stages. In contrast to the state of the art, CheckFree requires no additional computation or storage. However, because of the nature of averaging neighbouring stages, it can only recover failures of intermediate stages. We further extend our method to CheckFree+ with out-of-order pipeline execution to tolerate crashes of the first and last stages. Thanks to out-of-order pipelining, behaviour of the first and last stages are mimicked by their neighboring ones, which allows CheckFree+ to recover them by copying the neighboring stages. To recover the (de-)embedding layers, CheckFree+ copies those layers in the neighboring stages, which requires relatively small storage overhead. We extensively evaluate our method on LLaMa models of model sizes from 124M to 1.5B with varying failure frequencies. In the case of low and medium failure rates (5-10%), CheckFree and CheckFree+ outperform both checkpointing and redundant computation in terms of convergence wall-clock time, achieving up to 12% improvement over redundant computation. Both of our proposals can be ran via our code available at: https://github.com/gensyn-ai/CheckFree

## Compute & hardware (extracted from full text)

Estimates are normalised to H100-hours with the factors in [METHODOLOGY.md](../../../METHODOLOGY.md). Always check the quoted sentence: the parser can't tell which experiment a number belongs to.

| Est. H100-h | Parsed | Source sentence |
| --- | --- | --- |
| 0.22 | 8× H100 × 0.0278 h | For convergence tests we run on 2, 4, and 8 H100s for the small, medium, and large models respectively. |

Hardware setup mentions:

- We perform our tests on a single H100 node with 8 GPUs.

## Main results tables (from the paper)

**Table 1: Comparison of failure recovery strategies regarding the additional costs required even in the non-failure cases. $\mathcal{F}$ is the model, $\|S\|$ is the parameter size per stage, $\mathcal{E}$ is the embedding layer, and $N$ are the number of nodes.**

| Recovery method | Recovery method | Checkpoint [22] | RC [20] | CheckFree | CheckFree+ |
| --- | --- | --- | --- | --- | --- |
| Cost | Memory | $O(\|\mathcal{F}\|)$ | $O(\|\mathcal{F}\|)$ | 0 | $O(\|\mathcal{E}\|)$ |
| Cost | Communication | $O(\|\mathcal{F}\|)$ | $O(\|\mathcal{F}\|)$ | $0$ | $O(\|\mathcal{E}\|)$ |
| Cost | Computational | 0 | Forward pass | 0 | 0 |
| Cost | Non-faulty storage | Yes | No | No | No |
| Recovery | non-consecutive stages | Yes | Yes | Yes | Yes |
| Recovery | consecutive stages | Yes | No | No | No |
| Recovery | first and last stages | Yes | Yes | No | Yes |

**Table 2: Performance of three different recovery strategies for three different failure rates until convergence for medium models (specifically, when the validation loss reaches 2.85).**

|  | Checkpointing | Checkpointing | Checkpointing | Redundant Comp. | Redundant Comp. | Redundant Comp. | CheckFree | CheckFree | CheckFree | CheckFree+ | CheckFree+ | CheckFree+ |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Failure rate | 5% | 10% | 16% | 5% | 10% | 16% | 5% | 10% | 16% | 5% | 10% | 16% |
| Iteration time (s) | 91.35 | 91.35 | 92.14 | 151.04 | 151.04 | 151.04 | 91.32 | 91.32 | 92.12 | 91.32 | 91.32 | 92.12 |
| Train time (h) | 558.23 | 621.67 | 634.35 | 419.56 | 419.56 | 419.56 | 367.81 | 405.87 | 562.96 | 355.13 | 367.81 | 460.6 |

## Conclusion (from the paper)

Failure recovery is critical for enabling training of LLMs on distributed and unreliable nodes. Prior works recover stage failures within a pipeline through redundant computation or checkpointing. We propose CheckFree which recovers stage failures by reinitializing the failed stages with the weighted average of the neighboring stages. We further extend the protocol with CheckFree+ to tolerate first and last stage failures, via out of order pipelining. CheckFree exploits the inherent resilience of LLMs to layer omissions and efficient layer initialization techniques to recover from stage failures without requiring non-faulty storage or redundant computation. We extensively demonstrate the convergence of our method via empirical tests. We evaluate the effectiveness of CheckFree on LLaMa models of different sizes, showing that such lightweight recovery reduces the training time compared to the state of the art in 5% stage churn rate by over 12%. CheckFree+ also exhibits robust and steady convergence results for varying failure frequencies. Limitations and Future Work. Our proposed solutions CheckFree and CheckFree+ cannot recover from cases of consecutive stages failing together, as there is no neighboring stages for the reinitialization strategy. We believe that extending our methods with a lightweight checkpointing scheme will address this limitation and will explore this …

<details><summary>Paper outline</summary>

- 1 Introduction
- 2 Related Work
- 3 Problem Statement
- 4 Propose Recovery Method
- 4.1 Motivation
- 4.2 CheckFree: Memoryless Recovery
- 4.3 CheckFree+: First and Last Stage Recovery
- 4.4 Convergence Analysis of CheckFree
- 5 Evaluation
- 5.1 Convergence and Downstream Evaluation
- 5.2 Throughput with Failures
- 6 Conclusion
- Appendix A Reproducibility information
- A.1 Models
- A.2 Optimizers
- A.3 Datasets
- A.4 Hardware
- Appendix B Ablation Studies
- Appendix C Extended Proof

</details>

**Keywords:** LLMs, decentralized computation, node churn, checkpointing, redundant computation, out-of-order pipeline execution, LLaMa models, (de)embedding layers, wall-clock time convergence

## Reproduce / read

```sh
arxiv read 2506.15461            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2506.15461 "method"
arxiv bibtex 2506.15461
```
