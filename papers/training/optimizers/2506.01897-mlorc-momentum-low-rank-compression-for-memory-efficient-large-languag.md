# MLorc: Momentum Low-rank Compression for Memory Efficient Large Language Model Adaptation

**arXiv:** [2506.01897](https://arxiv.org/abs/2506.01897) · [PDF](https://arxiv.org/pdf/2506.01897) · [HTML](https://arxiv.org/html/2506.01897) · **First submitted:** 2026-04-25  
**Category:** [Optimizers & training dynamics (Muon, AdamW, schedules)](README.md) — **rank #128 of 158** by impact score  

> **TL;DR** — A novel memory-efficient training paradigm called Momentum Low-rank compression (MLorc), which compress and reconstruct the momentum of matrix parameters during training to reduce memory consumption and generalizes well across different optimizers.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Wei Shen, Zhang Yaxiang, Minhui Huang, Mengfan Xu, Jiawei Zhang, Cong Shen |
| arXiv categories | cs.LG, cs.IT, math.IT, math.OC |
| arXiv comments | AISTATS 2026 |
| Citations (Semantic Scholar, 2026-09-25) | 1 (0 influential) |
| Hugging Face upvotes | 0 |
| Code | — |
| Training requirement (from abstract) | involves (re)training / fine-tuning |
| Impact score | 0.57 |

## Key results (claimed in abstract)

- Empirically, MLorc consistently outperforms other memory-efficient training methods, matches or even exceeds the performance of full fine-tuning at small ranks (e.g., $r=4$), and generalizes well across different optimizers, all while not compromising time or memory efficiency.

## Abstract

With increasing size of large language models (LLMs), full-parameter fine-tuning imposes substantial memory demands. To alleviate this, we propose a novel memory-efficient training paradigm called Momentum Low-rank compression (MLorc). The key idea of MLorc is to compress and reconstruct the momentum of matrix parameters during training to reduce memory consumption. Compared to LoRA, MLorc avoids enforcing a fixed-rank constraint on weight update matrices and thus enables full-parameter learning. Compared to GaLore, MLorc directly compress the momentum rather than gradients, thereby better preserving the training dynamics of full-parameter fine-tuning. We provide a theoretical guarantee for its convergence under mild assumptions. Empirically, MLorc consistently outperforms other memory-efficient training methods, matches or even exceeds the performance of full fine-tuning at small ranks (e.g., $r=4$), and generalizes well across different optimizers, all while not compromising time or memory efficiency.

## Reproduce / read

```sh
arxiv read 2506.01897            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2506.01897 "method"
arxiv bibtex 2506.01897
```
