# Learning a Continue-Thinking Token for Enhanced Test-Time Scaling

**arXiv:** [2506.11274](https://arxiv.org/abs/2506.11274) · [PDF](https://arxiv.org/pdf/2506.11274) · [HTML](https://arxiv.org/html/2506.11274) · **First submitted:** 2026-05-12  
**Category:** [Test-time scaling & inference-time compute](README.md) — **rank #118 of 227** by impact score  

> **TL;DR** — This work augments a distilled version of DeepSeek-R1 with a single learned "|continue-thinking|>" token, training only its embedding via reinforcement learning while keeping the model weights frozen, and observes that in cases where the fixed-token approach enhances the base model's accuracy, this method achieves a markedly greater improvement.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Liran Ringel, Elad Tolochinsky, Yaniv Romano |
| arXiv categories | cs.CL, cs.LG |
| Venue | IJCNLP-AACL |
| Citations (Semantic Scholar, 2026-09-25) | 3 (0 influential) |
| Hugging Face upvotes | 5 |
| Code | https://github.com/liranringel/learning-continue-thinking-token |
| Training requirement (from abstract) | involves (re)training / fine-tuning |
| Models used | DeepSeek-R1 |
| Impact score | 3.98 |

## Key results (claimed in abstract)

- For example, on the GSM8K benchmark, the fixed-token approach yields a 1.3% absolute improvement in accuracy, whereas our learned-token method achieves a 4.2% improvement over the base model that does not use budget forcing.

## Abstract

Test-time scaling has emerged as an effective approach for improving language model performance by utilizing additional compute at inference time. Recent studies have shown that overriding end-of-thinking tokens (e.g., replacing "</think>" with "Wait") can extend reasoning steps and improve accuracy. In this work, we explore whether a dedicated continue-thinking token can be learned to trigger extended reasoning. We augment a distilled version of DeepSeek-R1 with a single learned "<|continue-thinking|>" token, training only its embedding via reinforcement learning while keeping the model weights frozen. Our experiments show that this learned token achieves improved accuracy on standard math benchmarks compared to both the baseline model and a test-time scaling approach that uses a fixed token (e.g., "Wait") for budget forcing. In particular, we observe that in cases where the fixed-token approach enhances the base model's accuracy, our method achieves a markedly greater improvement. For example, on the GSM8K benchmark, the fixed-token approach yields a 1.3% absolute improvement in accuracy, whereas our learned-token method achieves a 4.2% improvement over the base model that does not use budget forcing.

**Keywords:** test-time scaling, language model, end-of-thinking tokens, continue-thinking token, reinforcement learning, distilled version, GSM8K benchmark

## Reproduce / read

```sh
arxiv read 2506.11274            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2506.11274 "method"
arxiv bibtex 2506.11274
```
