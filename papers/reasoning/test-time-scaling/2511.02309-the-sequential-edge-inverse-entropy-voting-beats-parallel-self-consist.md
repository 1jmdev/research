# The Sequential Edge: Inverse-Entropy Voting Beats Parallel Self-Consistency at Matched Compute

**arXiv:** [2511.02309](https://arxiv.org/abs/2511.02309) · [PDF](https://arxiv.org/pdf/2511.02309) · [HTML](https://arxiv.org/html/2511.02309) · **First submitted:** 2025-11-04  
**Category:** [Test-time scaling & inference-time compute](README.md) — **rank #103 of 227** by impact score  

> **TL;DR** — It is found that sequential scaling where chains explicitly build upon previous attempts consistently outperforms the dominant parallel self-consistency paradigm in 95.6% of configurations with gains in accuracy upto 46.7% and is established as the optimal test-time scaling strategy.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Aman Sharma, Paras Chopra |
| Organisation (HF) | Lossfunk |
| arXiv categories | cs.LG, cs.AI |
| Citations (Semantic Scholar, 2026-09-25) | 10 (1 influential) |
| Hugging Face upvotes | 4 |
| Code | — |
| Training requirement (from abstract) | training-free / post-training |
| Impact score | 4.64 |

## Key results (claimed in abstract)

- Through comprehensive evaluation across 5 state-of-the-art open source models and 3 challenging reasoning benchmarks, we find that sequential scaling where chains explicitly build upon previous attempts consistently outperforms the dominant parallel self-consistency paradigm in 95.6% of configurations with gains in accuracy upto 46.7%.

## Abstract

We revisit test-time scaling for language model reasoning and ask a fundamental question: at equal token budget and compute, is it better to run multiple independent chains in parallel, or to run fewer chains that iteratively refine through sequential steps? Through comprehensive evaluation across 5 state-of-the-art open source models and 3 challenging reasoning benchmarks, we find that sequential scaling where chains explicitly build upon previous attempts consistently outperforms the dominant parallel self-consistency paradigm in 95.6% of configurations with gains in accuracy upto 46.7%. Further, we introduce inverse-entropy weighted voting, a novel training-free method to further boost the accuracy of sequential scaling. By weighing answers in proportion to the inverse entropy of their reasoning chains, we increase our success rate over parallel majority and establish it as the optimal test-time scaling strategy. Our findings fundamentally challenge the parallel reasoning orthodoxy that has dominated test-time scaling since Wang et al.'s self-consistency decoding (Wang et al., 2022), positioning sequential refinement as the robust default for modern LLM reasoning and necessitating a paradigm shift in how we approach inference-time optimization.

**Keywords:** test-time scaling, language model reasoning, token budget, compute, independent chains, sequential steps, state-of-the-art models, reasoning benchmarks, sequential scaling, parallel self-consistency, inverse-entropy weighted voting, reasoning chains

## Reproduce / read

```sh
arxiv read 2511.02309            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2511.02309 "method"
arxiv bibtex 2511.02309
```
