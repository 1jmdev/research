# Doc-to-LoRA: Learning to Instantly Internalize Contexts

**arXiv:** [2602.15902](https://arxiv.org/abs/2602.15902) · [PDF](https://arxiv.org/pdf/2602.15902) · [HTML](https://arxiv.org/html/2602.15902) · **First submitted:** 2026-02-13  
**Category:** [Prompt & context compression](README.md) — **rank #13 of 112** by impact score  

> **TL;DR** — On a long-context needle-in-a-haystack task, D2L successfully learns to map contexts into adapters that store the needle information, achieving near-perfect zero-shot accuracy at sequence lengths exceeding the target LLM's native context window by more than 4x.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Rujikorn Charakorn, Edoardo Cetin, Shinnosuke Uesaka, Robert Tjarko Lange |
| arXiv categories | cs.CL, cs.AI |
| Citations (Semantic Scholar, 2026-09-25) | 31 (4 influential) |
| Hugging Face upvotes | 0 |
| Code | — |
| Training requirement (from abstract) | involves (re)training / fine-tuning |
| Models used | gemma-2-2b, Mistral-7B, Qwen3-4B, gemma-3-12b, gemma-3-4b |
| Hardware | H200 (×4 mentions) |
| **Compute (est. H100-hours)** | **3.3 – 1.1k** (auto-extracted, see below) |
| Largest token count mentioned | 900M tokens |
| Impact score | 6.17 |

## Key results (claimed in abstract)

- Given an unseen prompt, D2L generates a LoRA adapter for a target LLM, enabling subsequent queries to be answered without re-consuming the original context, reducing latency and KV-cache memory consumption during inference of the target LLM.
- On a long-context needle-in-a-haystack task, D2L successfully learns to map contexts into adapters that store the needle information, achieving near-perfect zero-shot accuracy at sequence lengths exceeding the target LLM's native context window by more than 4x.
- On real-world QA datasets with limited compute, D2L outperforms standard CD while significantly reducing peak memory consumption and update latency.

## Abstract

Long input sequences are central to in-context learning, document understanding, and multi-step reasoning of Large Language Models (LLMs). However, the quadratic attention cost of Transformers makes inference memory-intensive and slow. While context distillation (CD) can transfer information into model parameters, per-prompt distillation is impractical due to training costs and latency. To address these limitations, we propose Doc-to-LoRA (D2L), a lightweight hypernetwork that meta-learns to perform approximate CD within a single forward pass. Given an unseen prompt, D2L generates a LoRA adapter for a target LLM, enabling subsequent queries to be answered without re-consuming the original context, reducing latency and KV-cache memory consumption during inference of the target LLM. On a long-context needle-in-a-haystack task, D2L successfully learns to map contexts into adapters that store the needle information, achieving near-perfect zero-shot accuracy at sequence lengths exceeding the target LLM's native context window by more than 4x. On real-world QA datasets with limited compute, D2L outperforms standard CD while significantly reducing peak memory consumption and update latency. We envision that D2L can facilitate rapid adaptation of LLMs, opening up the possibility of frequent knowledge updates and personalized chat behavior.

## Contributions (from the paper)

To enable D2L to internalize contexts longer than those seen during training, we base our hypernetwork on the Perceiver architecture (Jaegle et al., 2021), allowing it to map variable-length inputs to a fixed-shape LoRA adapter. Furthermore, D2L utilizes a chunking mechanism that allows it to produce higher-rank LoRA adapters for longer contexts without changing the output shape of the hypernetwork. This mechanism is crucial for processing documents longer than the context window of the target LLM. As a result, on a synthetic Needle-in-a-Haystack (NIAH) task, D2L achieves near-perfect zero-shot accuracy on contexts beyond 32K tokens, despite being trained only on sequences of up to 256 tokens, indicating that D2L is capable of generalizing beyond the lengths of its training data and that the chunking mechanism is effective at processing long sequences. Empirically, on real-world QA tasks, D2L learns an effective mapping between contexts and their corresponding LoRA adapters, outperforming CD under low compute budgets while significantly reducing internalization latency and compute cost. On long-document QA tasks, D2L generalizes in a zero-shot manner to documents exceeding the training length, without being explicitly trained to internalize such long inputs. A conceptual overview of D2L is shown in Figure 1. We summarize our contributions as follows: We introduce a meta-training objective that distills the CD process into a hypernetwork. This approach amortizes the overhead associated with traditional CD by training the hypernetwork to emulate the entire CD process in a sin …

## Compute & hardware (extracted from full text)

Estimates are normalised to H100-hours with the factors in [METHODOLOGY.md](../../../METHODOLOGY.md). Always check the quoted sentence: the parser can't tell which experiment a number belongs to.

| Est. H100-h | Parsed | Source sentence |
| --- | --- | --- |
| 3.3 | 1× H200 × 3 h | The meta-training takes around 3 hours on a single H200 GPU. |
| 1.1k | 8× H200 × 120 h | Specifically, an entire training run for meta-learning CD for gemma-2-2b-it takes around 5 days on 8 H200 GPUs. |

Hardware setup mentions:

- All evaluations and measurements are done with a single H200 GPU.

## Main results tables (from the paper)

**Table 1: Performance, update memory, and latency of in-parameter knowledge methods on 2WikiMultihopQA benchmark.**

| Method | Normalized Performance ($\uparrow$) | Additional Update Memory (GB, $\downarrow$) | Mean Update Latency (s, $\downarrow$) |
| --- | --- | --- | --- |
| Normalized |  |  |  |
| Performance ($\uparrow$) |  |  |  |
| Additional Update |  |  |  |
| Memory (GB, $\downarrow$) |  |  |  |
| Mean Update |  |  |  |
| Latency (s, $\downarrow$) |  |  |  |
| CD (oracle query) | $0.901$ | $7.820$ | $40.171\pm 0.351$ |
| D2L (batched) | $\mathbf{0.857}$ | $11.522$ | $\mathbf{0.209}\pm 0.123$ |
| D2L (iterative) | $0.844$ | $\mathbf{3.791}$ | $0.551\pm 0.101$ |
| CD (25 generated queries) | ${0.745}$ | ${59.925}$ | $465.454\pm 67.868$ |
| CD (5 generated queries) | $0.704$ | $79.371$ | $72.537\pm 7.821$ |
| Normalized |
| --- |
| Performance ($\uparrow$) |
| Additional Update |
| --- |
| … |

**Table 4: SQuAD (100 samples) performance with varying queries.**

| Method | Normalized Performance | Update Latency (s) |
| --- | --- | --- |
| CD (oracle) | ${0.988}$ | ${8.763\pm 0.313}$ |
| D2L | 0.866 | ${\textbf{0.086}\pm 0.061}$ |
| CD (100 generated queries) | ${0.650}$ | ${631.101\pm 12.879}$ |
| CD (50 generated queries) | ${0.601}$ | ${311.661\pm 7.211}$ |
| CD (20 generated queries) | ${0.506}$ | ${129.044\pm 5.813}$ |

<details><summary>Paper outline</summary>

- 1 Introduction
- 2 Preliminaries
- 3 Meta-Learning Context Distillation
- 3.1 Learning to Internalize Context with Hypernetworks
- 3.2 D2L Architecture
- 4 Implanting Synthetic Needle-in-a-Haystack Information
- 5 Experiments
- 5.1 Question-Answering from Implanted Knowledge
- 5.2 Zero-Shot Internalization of Visual Information
- 6 Analyses
- 7 Related Work
- Acknowledgement
- Impact Statement
- Appendix A NIAH experiment details
- Appendix B Main Experiments Details
- B.1 Meta-Training Data Generation Pipeline
- B.2 Training and Experimental Details
- Appendix C Additional Results (Gemma-2-2B)
- C.1 Data Ablation
- C.2 Ablating Training Objective
- C.3 Increasing LoRA Rank
- C.4 Knowledge Interference
- Appendix D Generating KV cache Instead of LoRA (Qwen3-4B-Instruct-2507)
- Appendix E Results with Other Models
- Appendix F Related Work (Extended)
- Appendix G Conclusion
- Appendix H LLMs Usage Disclosure

</details>

## Reproduce / read

```sh
arxiv read 2602.15902            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2602.15902 "method"
arxiv bibtex 2602.15902
```
