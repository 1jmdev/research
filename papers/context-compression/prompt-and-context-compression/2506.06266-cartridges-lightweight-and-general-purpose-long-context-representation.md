# Cartridges: Lightweight and general-purpose long context representations via self-study

**arXiv:** [2506.06266](https://arxiv.org/abs/2506.06266) · [PDF](https://arxiv.org/pdf/2506.06266) · [HTML](https://arxiv.org/html/2506.06266) · **First submitted:** 2025-06-13  
**Category:** [Prompt & context compression](README.md) — **rank #8 of 112** by impact score  
**Also relevant to:** [KV cache — general, systems & analysis](../../kv-cache/_general/README.md)  

> **TL;DR** — Self-study is proposed, a training recipe in which synthetic conversations about the corpus are generated and the Cartridge is trained with a context-distillation objective, and it is found that Cartridges trained with self-study replicate the functionality of ICL, while being significantly cheaper to serve.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Sabri Eyuboglu, Ryan Ehrlich, Simran Arora, Neel Guha, Dylan Zinsley, Emily Liu, Will Tennien, Atri Rudra, James Zou, Azalia Mirhoseini, … (+1) |
| arXiv categories | cs.CL, cs.AI, cs.LG |
| Citations (Semantic Scholar, 2026-09-25) | 65 (9 influential) |
| Hugging Face upvotes | 7 |
| Code | https://github.com/HazyResearch/cartridges |
| Training requirement (from abstract) | training-free / post-training; involves (re)training / fine-tuning |
| Models used | Llama, Llama-3, Phi-4, GPT-4o, Falcon, Llama-3-8B, GPT-4 |
| Hardware | H100 (×3 mentions), H20 (×1 mentions) |
| Largest token count mentioned | 1M tokens |
| Impact score | 10.72 |

## Key results (claimed in abstract)

- Although current models support contexts of 100K-1M tokens, this setup is costly to serve because the memory consumption of the KV cache scales with input length.
- On challenging long-context benchmarks, Cartridges trained with self-study match ICL performance while using 38.6x less memory and enabling 26.4x higher throughput.

## Abstract

Large language models are often used to answer queries grounded in large text corpora (e.g. codebases, legal documents, or chat histories) by placing the entire corpus in the context window and leveraging in-context learning (ICL). Although current models support contexts of 100K-1M tokens, this setup is costly to serve because the memory consumption of the KV cache scales with input length. We explore an alternative: training a smaller KV cache offline on each corpus. At inference time, we load this trained KV cache, which we call a Cartridge, and decode a response. Critically, the cost of training a Cartridge can be amortized across all the queries referencing the same corpus. However, we find that the naive approach of training the Cartridge with next-token prediction on the corpus is not competitive with ICL. Instead, we propose self-study, a training recipe in which we generate synthetic conversations about the corpus and train the Cartridge with a context-distillation objective. We find that Cartridges trained with self-study replicate the functionality of ICL, while being significantly cheaper to serve. On challenging long-context benchmarks, Cartridges trained with self-study match ICL performance while using 38.6x less memory and enabling 26.4x higher throughput. Self-study also extends the model's effective context length (e.g. from 128k to 484k tokens on MTOB) and surprisingly, leads to Cartridges that can be composed at inference time without retraining.

## Compute & hardware (extracted from full text)

Hardware setup mentions:

- We measure throughput on a single H100 GPU.

## Conclusion (from the paper)

We propose Cartridges as an alternative to ICL for settings where many different user messages reference the same large corpus of text. We demonstrate across a diverse set of language model workloads that, when trained via Self-Study, they match ICL’s response quality while substantially reducing memory consumption (38.6\times memory reduction across our evaluations) and increasing peak throughput (26.4\times higher tokens per second). Cartridges are simple to train, composable, and compatible with existing LLM serving infrastructure. However, compared with ICL, Self-Study is not without limitations. Using Self-Study to produce a KV-cache is much more costly than simply running standard ICL pre-fill. With our unoptimized implementation, training an ICL-quality Cartridge takes \sim 30 minutes on a single 8\timesH100 node (for Llama-8B) So our work does not provide a drop-in replacement for ICL, but rather demonstrates one way to tradeoff increased compute for reduced memory when constructing a KV-cache. This tradeoff is extremely advantageous in many settings: users often issue many queries over the same corpus and Self-Study can be trained offline on idle or underutilized compute (e.g. at night when user load is low [39, 29]). Furthermore, there is ample room for optimizations (e.g. improved shared-prefix attention kernels [22, 103, 44]) that would make Self-Study training …

<details><summary>Paper outline</summary>

- 1 Introduction
- 2 Preliminaries
- 2.1 Related work
- 2.2 Problem setup
- 2.3 Language models and KV caches
- 3 The Cartridge paradigm
- 3.1 Formalizing Cartridges
- 3.2 Parameterizing Cartridges
- 3.3 Serving Cartridges
- 4 Self-Study: A self-supervised method for training Cartridges
- 4.1 Self-supervised synthetic data to avoid overfitting
- 4.2 Self-Study context-distillation objective
- 5 Results
- 5.1 Pushing the quality/cost tradeoff frontier
- 5.2 Extending the effective context window
- 5.3 Ablating Self-Study design choices
- 5.4 Composing Cartridges
- 6 Discussion and conclusion
- Appendix A Extended Results
- A.1 Cartridge design choices: parameterization and initialization
- A.2 Self-Study design choices: data-generation and objective
- A.3 Throughput measurement details
- Appendix B Extended Related Work
- B.1 Prior work related to the parameterization of Cartridges
- B.2 Prior work related to Self-Study
- B.3 Reducing the size of the KV cache
- Appendix C Extended method description
- C.1 Self-Study seed prompts
- C.2 Self-Study chunking
- Appendix D Datasets
- D.1 GenConvo
- D.2 LongHealth
- D.3 MTOB
- D.4 QASPER
- Appendix E Theoretical analysis: Relationship between attention, linear attention, and Cartridges
- E.1 Notation
- E.2 MQAR
- E.3 $\mathbf{m}-\text{repetitive MQAR}$
- E.4 Model Definitions

</details>

**Keywords:** KV cache, Cartridge, in-context learning (ICL), self-study, context-distillation objective, MTOB

## Reproduce / read

```sh
arxiv read 2506.06266            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2506.06266 "method"
arxiv bibtex 2506.06266
```
