# DeepSeek-OCR: Contexts Optical Compression

**arXiv:** [2510.18234](https://arxiv.org/abs/2510.18234) · [PDF](https://arxiv.org/pdf/2510.18234) · [HTML](https://arxiv.org/html/2510.18234) · **First submitted:** 2025-10-21  
**Category:** [Prompt & context compression](README.md) — **rank #1 of 109** by impact score  
**Also relevant to:** [Visual token pruning / merging for VLMs](../visual-token-reduction/README.md) · [Long context, context extension & positional encoding](../../attention/long-context-and-position/README.md)  

> **TL;DR** — DeepSeek-OCR shows considerable promise for research areas such as historical long-context compression and memory forgetting mechanisms in LLMs, and demonstrates high practical value on OmniDocBench.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Haoran Wei, Yaofeng Sun, Yukun Li |
| Organisation (HF) | DeepSeek |
| arXiv categories | cs.CV |
| Citations (Semantic Scholar, 2026-09-25) | 189 (20 influential) |
| Hugging Face upvotes | 95 |
| Code | http://github.com/deepseek-ai/DeepSeek-OCR |
| Hardware | A100 |
| Impact score | 18.35 |

## Key results (claimed in abstract)

- Experiments show that when the number of text tokens is within 10 times that of vision tokens (i.e., a compression ratio < 10x), the model can achieve decoding (OCR) precision of 97%.
- Even at a compression ratio of 20x, the OCR accuracy still remains at about 60%.
- On OmniDocBench, it surpasses GOT-OCR2.0 (256 tokens/page) using only 100 vision tokens, and outperforms MinerU2.0 (6000+ tokens per page on average) while utilizing fewer than 800 vision tokens.

## Abstract

We present DeepSeek-OCR as an initial investigation into the feasibility of compressing long contexts via optical 2D mapping. DeepSeek-OCR consists of two components: DeepEncoder and DeepSeek3B-MoE-A570M as the decoder. Specifically, DeepEncoder serves as the core engine, designed to maintain low activations under high-resolution input while achieving high compression ratios to ensure an optimal and manageable number of vision tokens. Experiments show that when the number of text tokens is within 10 times that of vision tokens (i.e., a compression ratio < 10x), the model can achieve decoding (OCR) precision of 97%. Even at a compression ratio of 20x, the OCR accuracy still remains at about 60%. This shows considerable promise for research areas such as historical long-context compression and memory forgetting mechanisms in LLMs. Beyond this, DeepSeek-OCR also demonstrates high practical value. On OmniDocBench, it surpasses GOT-OCR2.0 (256 tokens/page) using only 100 vision tokens, and outperforms MinerU2.0 (6000+ tokens per page on average) while utilizing fewer than 800 vision tokens. In production, DeepSeek-OCR can generate training data for LLMs/VLMs at a scale of 200k+ pages per day (a single A100-40G). Codes and model weights are publicly accessible at http://github.com/deepseek-ai/DeepSeek-OCR.

**Keywords:** DeepSeek-OCR, DeepEncoder, DeepSeek3B-MoE-A570M, optical 2D mapping, vision tokens, OCR precision, compression ratio, OmniDocBench, GOT-OCR2.0, MinerU2.0, LLMs, VLMs

## Reproduce / read

```sh
arxiv read 2510.18234            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2510.18234 "method"
arxiv bibtex 2510.18234
```
