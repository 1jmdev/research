# OmniScope: Modality-Decoupled Token Compression for Omnimodal Large Language Models

**arXiv:** [2607.23193](https://arxiv.org/abs/2607.23193) · [PDF](https://arxiv.org/pdf/2607.23193) · [HTML](https://arxiv.org/html/2607.23193) · **First submitted:** 2026-08-02  
**Category:** [Visual token pruning / merging for VLMs](README.md) — **rank #88 of 301** by impact score  

> **TL;DR** — This work proposes OmniScope, a training-free token compression framework that uses the query as a shared semantic anchor while estimating relevance separately for audio and video, and suggests a simple design principle for OmniLLM inference: share the query across modalities, but not the salience estimates.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Jinsen Su, Yongdong Luo, Yuexiao Ma, Yibo Hu, Meiguang Jin, Xiawu Zheng |
| arXiv categories | cs.CV |
| Citations (Semantic Scholar, 2026-09-25) | 0 (0 influential) |
| Hugging Face upvotes | 7 |
| Code | https://github.com/MAC-AutoML/OmniScope |
| Training requirement (from abstract) | training-free / post-training |
| Models used | Qwen2.5 |
| Impact score | 2.87 |

## Key results (claimed in abstract)

- Across four audio-video benchmarks and two Qwen2.5-Omni model scales, OmniScope achieves the best average accuracy across all compression settings.
- At 25% overall token retention, it delivers up to 3.53x prefill speedup and more than 15% GPU memory reduction, with only a 0.35-point drop in average accuracy.

## Abstract

Existing token compression methods for omnimodal large language models typically rely on one modality to determine what to retain in the other. We show that this assumption often breaks down: for the same query, audio and video relevance often peaks at different moments. This cross-modal salience mismatch makes unidirectional guidance prone to discarding answer-critical cues under aggressive compression. We propose OmniScope, a training-free token compression framework that uses the query as a shared semantic anchor while estimating relevance separately for audio and video. OmniScope allocates modality-specific token budgets, prunes visual tokens with an anchor-delta strategy that preserves both global context and temporal changes, and merges audio tokens within each second to reduce redundancy while maintaining temporal continuity. Across four audio-video benchmarks and two Qwen2.5-Omni model scales, OmniScope achieves the best average accuracy across all compression settings. At 25% overall token retention, it delivers up to 3.53x prefill speedup and more than 15% GPU memory reduction, with only a 0.35-point drop in average accuracy. These results suggest a simple design principle for OmniLLM inference: share the query across modalities, but not the salience estimates. The code is available at https://github.com/MAC-AutoML/OmniScope.

**Keywords:** omnimodal large language models, token compression, cross-modal salience mismatch, OmniScope, anchor-delta strategy, modality-specific token budgets, audio-video benchmarks, Qwen2.5-Omni

## Reproduce / read

```sh
arxiv read 2607.23193            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2607.23193 "method"
arxiv bibtex 2607.23193
```
