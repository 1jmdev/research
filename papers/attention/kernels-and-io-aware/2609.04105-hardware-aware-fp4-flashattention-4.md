# Hardware-Aware FP4 FlashAttention-4

**arXiv:** [2609.04105](https://arxiv.org/abs/2609.04105) · [PDF](https://arxiv.org/pdf/2609.04105) · [HTML](https://arxiv.org/html/2609.04105) · **First submitted:** 2026-09-03  
**Category:** [Attention kernels & IO-aware implementations](README.md) — **rank #44 of 49** by impact score  

> **TL;DR** — Direct-P is introduced for noncausal inference and a causal path that passes the forward quantization directly into backward into forward quantization is addressed, accelerating a complete single-GPU 8-billion-parameter update by up to 1.14$\times$.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Robert Hu |
| arXiv categories | cs.LG |
| Citations (Semantic Scholar, 2026-09-25) | 0 (0 influential) |
| Hugging Face upvotes | 0 |
| Code | — |
| Hardware | GB200 |
| Impact score | 0.0 |

## Key results (claimed in abstract)

- Blackwell's 4-bit floating-point (FP4) tensor cores do not automatically make attention faster because softmax conversion and on-chip dependencies dominate once its matrix products shrink.
- Direct-P maps scores directly to FP4 probabilities and reaches up to 2.13$\times$ the bfloat16 (BF16) forward throughput on an NVIDIA GB200.
- The causal path reconstructs probabilities from saved quantized queries and keys and uses 8-bit floating-point (FP8) gradient operands, accelerating a complete single-GPU 8-billion-parameter update by up to 1.14$\times$.

## Abstract

Blackwell's 4-bit floating-point (FP4) tensor cores do not automatically make attention faster because softmax conversion and on-chip dependencies dominate once its matrix products shrink. We address this with \emph{Direct-P} for noncausal inference and a causal path that passes the forward quantization directly into backward. Direct-P maps scores directly to FP4 probabilities and reaches up to 2.13$\times$ the bfloat16 (BF16) forward throughput on an NVIDIA GB200. The causal path reconstructs probabilities from saved quantized queries and keys and uses 8-bit floating-point (FP8) gradient operands, accelerating a complete single-GPU 8-billion-parameter update by up to 1.14$\times$. Matched distributed training retains FP8 probabilities and values; every tested MXFP4 probability/value training trajectory diverges.

## Reproduce / read

```sh
arxiv read 2609.04105            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2609.04105 "method"
arxiv bibtex 2609.04105
```
