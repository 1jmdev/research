# PAT: Accelerating LLM Decoding via Prefix-Aware Attention with Resource Efficient Multi-Tile Kernel

**arXiv:** [2511.22333](https://arxiv.org/abs/2511.22333) · [PDF](https://arxiv.org/pdf/2511.22333) · [HTML](https://arxiv.org/html/2511.22333) · **First submitted:** 2026-03-16  
**Category:** [Attention kernels & IO-aware implementations](README.md) — **rank #9 of 49** by impact score  
**Also relevant to:** [Memory management & offloading (weights, activations)](../../serving-systems/memory-and-offloading/README.md) · [KV cache — general, systems & analysis](../../kv-cache/_general/README.md)  

> **TL;DR** — PAT is a prefix-aware attention kernel implementation for LLM decoding that organizes execution with a pack-forward-merge paradigm that packs queries by shared prefix to reduce repeated memory accesses, runs a customized multi-tile kernel to achieve high resource efficiency, and applies practical multi-stream forwarding and KV splitting to reduce resource bubbles.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Jinjun Yi, Zhixin Zhao, Yitao Hu, Ke Yan, Weiwei Sun, Hao Wang, Laiping Zhao, Yuhao Zhang, Wenxin Li, Keqiu Li |
| arXiv categories | cs.DC, cs.CL |
| Venue | Accepted by ASPLOS'26 |
| arXiv comments | Accepted by ASPLOS'26, code available at https://github.com/flashserve/PAT |
| Citations (Semantic Scholar, 2026-09-25) | 5 (1 influential) |
| Hugging Face upvotes | 0 |
| Code | https://github.com/flashserve/PAT |
| Models used | Llama-3-8B, Qwen3-8B, Qwen2.5-72B, Qwen3-30B, Llama3, Qwen3, Llama-4, Llama, Gemma, Llama3-8B |
| Hardware | A100 (×14 mentions), H100 (×6 mentions), B200 (×1 mentions), V100 (×1 mentions) |
| **Compute (est. H100-hours)** | **10 – 60** (auto-extracted, see below) |
| Impact score | 3.68 |

## Key results (claimed in abstract)

- Evaluation on both real-world and synthetic workloads shows that PAT reduces attention latency by 53.5% on average and TPOT by 17.0-93.1% under the same configurations against state-of-the-art attention kernels.

## Abstract

LLM serving is increasingly dominated by decode attention, which is a memory-bound operation due to massive KV cache loading from global memory. Meanwhile, real-world workloads exhibit substantial, hierarchical shared prefixes across requests (e.g., system prompts, tools/templates, RAG). Existing attention implementations fail to fully exploit prefix sharing: one-query-per-CTA execution repeatedly loads shared prefix KV cache, while one-size-fits-all tiling leaves on-chip resources idle and exacerbates bubbles for uneven KV lengths. These choices amplify memory bandwidth pressure and stall memory-bound decode attention. This paper introduces PAT, a prefix-aware attention kernel implementation for LLM decoding that organizes execution with a pack-forward-merge paradigm. PAT packs queries by shared prefix to reduce repeated memory accesses, runs a customized multi-tile kernel to achieve high resource efficiency. It further applies practical multi-stream forwarding and KV splitting to reduce resource bubbles. The final merge performs online softmax with negligible overhead. We implement PAT as an off-the-shelf plugin for vLLM. Evaluation on both real-world and synthetic workloads shows that PAT reduces attention latency by 53.5% on average and TPOT by 17.0-93.1% under the same configurations against state-of-the-art attention kernels. PAT's source code is publicly available at https://github.com/flashserve/PAT.

## Compute & hardware (extracted from full text)

Estimates are normalised to H100-hours with the factors in [METHODOLOGY.md](../../../METHODOLOGY.md). Always check the quoted sentence: the parser can't tell which experiment a number belongs to.

| Est. H100-h | Parsed | Source sentence |
| --- | --- | --- |
| 60 | 60 unspecified GPU-h | Note that completing all experiments requires over 60 GPU-hours, so we provide two scripts for convenience: (1) run_e2e_bench_part.sh: runs a subset of experiments (QPS=7&9, all workloads, all baselines) for quick verification; (2) run_e2e_bench_full.sh: runs all experiments in Figure 12. ⬇ $ cd /workspace/PAT/benchmark $ # Quick verification (8-10 GPU-hours) $ bash ./run_e2e_bench_part.sh $ # Full experiments (over  |
| 10 | 10 unspecified GPU-h | Note that completing all experiments requires over 60 GPU-hours, so we provide two scripts for convenience: (1) run_e2e_bench_part.sh: runs a subset of experiments (QPS=7&9, all workloads, all baselines) for quick verification; (2) run_e2e_bench_full.sh: runs all experiments in Figure 12. ⬇ $ cd /workspace/PAT/benchmark $ # Quick verification (8-10 GPU-hours) $ bash ./run_e2e_bench_part.sh $ # Full experiments (over  |

Hardware setup mentions:

- For kernel benchmark (§8.3), we evaluate PAT on both NVIDIA A100 GPU (80GB) and NVIDIA H100 GPU.
- We further evaluate PAT under distributed settings and Mixture-of-Experts (MoE) architectures in §8.5 using Qwen2.5-72B-Instruct on four A100 GPUs and Qwen3-30B-A3B on one A100 GPU.
- Figure 11 reports normalized kernel performance (metric \text{latency}^{-1}, normalized to PAT) across decode-batch configurations on NVIDIA A100 GPU (80GB) 7 Evaluation results on NVIDIA H100 GPU are presented in Appendix A..
- To validate these properties, we evaluate PAT under the toolagent trace using Qwen2.5-72B-Instruct with TP=2 and PP=2 on four A100 GPUs, and Qwen3-30B-A3B on one A100 GPU.

## Main results tables (from the paper)

**Table 1. Memory hierarchy of A100-SXM4-80GB (NVIDIA, 2020; Abdelkhalik et al., 2022).**

| Level | Shared By | Size | Latency | Bandwidth∗ | Type |
| --- | --- | --- | --- | --- | --- |
| Register | Thread | 256KB/SM† | $\sim$2ns | $\sim$20 TB/s | on-chip |
| Shared Memory / L1 Cache | CTA | 192KB/SM‡ | $\sim$20ns | $\sim$19 TB/s | on-chip |
| L2 Cache | All SMs | 40MB | $\sim$140ns | $\sim$2 TB/s | on-chip |
| Global Memory | All SMs | 80GB | $\sim$200ns | $\sim$2 TB/s | off-chip |
| ∗Read/write bandwidth from the upper memory level. | ∗Read/write bandwidth from the upper memory level. | ∗Read/write bandwidth from the upper memory level. | ∗Read/write bandwidth from the upper memory level. | ∗Read/write bandwidth from the upper memory level. | ∗Read/write bandwidth from the upper memory level. |
| †Each thread is limited to 255 registers (4*8 bits each). | †Each thread is limited to 255 registers (4*8 bits each). | †Each thread is limited to 255 registers (4*8 bits each). | †Each thread is limited to 255 registers (4*8 bits each). | †Each thread is limited to 255 registers (4*8 bits each). | †Each thread is limited to 255 registers (4*8 bits each). |
| ‡Each CTA can address up to 163 KB of shared memory. | ‡Each CTA can address up to 163 KB of shared memory. | ‡Each CTA can address up to 163 KB of shared memory. | ‡Each CTA can address up to 163 KB of shared memory. | ‡Each CTA can address up to 163 KB of shared memory. | ‡Each CTA can address up to 163 KB of shared memory. |

## Conclusion (from the paper)

In this work, we present PAT, a prefix-aware attention kernel implementation for LLM decoding. PAT reduces redundant global memory accesses and improves bandwidth utilization through a pack–forward–merge paradigm. The design incorporates a heuristic pack scheduler, a resource-efficient multi-tile kernel, and forward-stage optimizations that mitigate execution bubbles. We implement PAT as an off-the-shelf plugin to vLLM and demonstrate that it reduces the attention latency by 53.5% on average under synthetic workloads and reduces the TPOT by up to 93.1% under real-world workloads compared with existing works.

<details><summary>Paper outline</summary>

- Abstract.
- 1. Introduction
- 2. Background
- 2.1. LLM Inference and Attention
- 2.2. Memory Bottleneck of Attention
- 2.3. GPU Execution Model
- 3. Motivation
- 3.1. Shared Prefixes and Prefix Reuse
- 3.2. Redundant Memory Access
- 3.3. Two-Dimension Resource Inefficiencies
- 3.4. Insight: Pack-forward-merge Paradigm
- 3.5. Challenges
- 4. Overview
- 5. Pack Scheduler
- 5.1. Pack Scheduler
- 5.2. Multi-tile Kernel
- 6. Multi Kernel Forward
- 7. Output Merge
- 8. Evaluation
- 8.1. Implementation of PAT
- 8.2. Experiment Setup
- 8.3. Kernel Performance
- 8.4. End-to-End Comparison
- 8.5. Distributed and MoE Extensions
- 8.6. Ablation Study
- 8.7. Overhead Analysis
- 9. Discussion
- 10. Related Work
- 11. Conclusion
- 12. Acknowledgments
- Appendix A Kernel Performance on H100 GPU
- Appendix B Artifact Appendix
- B.1. Abstract
- B.2. Artifact check-list (meta-information)
- B.3. Description
- B.4. Installation
- B.5. Experiment workflow
- B.6. Evaluation and expected results

</details>

## Reproduce / read

```sh
arxiv read 2511.22333            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2511.22333 "method"
arxiv bibtex 2511.22333
```
