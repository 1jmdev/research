# FlashMLA-ETAP: Efficient Transpose Attention Pipeline for Accelerating MLA Inference on NVIDIA H20 GPUs

**arXiv:** [2506.01969](https://arxiv.org/abs/2506.01969) · [PDF](https://arxiv.org/pdf/2506.01969) · [HTML](https://arxiv.org/html/2506.01969) · **First submitted:** 2026-06-02  
**Category:** [Attention kernels & IO-aware implementations](README.md) — **rank #10 of 49** by impact score  
**Also relevant to:** [Attention variants (MLA, GQA/MQA, differential, gated, sinks)](../attention-variants/README.md)  

> **TL;DR** — A novel framework that enhances MLA inference for the single-instance deployment scenario on NVIDIA H20 GPUs, and proposes the Efficient Transpose Attention Pipeline (ETAP), which reconfigures attention computation through transposition to align the KV context length with the \(M\)-dimension in WGMMA operations, significantly reducing redundant computations.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Pengcuo Dege, Qiuming Luo, Rui Mao, Chang Kong |
| arXiv categories | cs.DC, cs.AI, cs.LG |
| arXiv comments | Accepted by ICONIP2025 |
| Citations (Semantic Scholar, 2026-09-25) | 5 (1 influential) |
| Hugging Face upvotes | 0 |
| Code | https://github.com/pengcuo/FlashMLA-ETAP |
| Models used | DeepSeek-R1, DeepSeek-V3, Mamba, RWKV |
| Hardware | H20 (×39 mentions), H800 (×4 mentions), H100 (×4 mentions) |
| Impact score | 3.49 |

## Key results (claimed in abstract)

- FlashMLA-ETAP achieves a 2.78x speedup over FlashMLA at 64K sequence length (batch size 16), with 5.24x and 4.94x improvements over FlashAttention-3 and FlashInfer, respectively, while maintaining numerical stability with a 15.2x lower RMSE (\(1.25 \times 10^{-5}\)) than FlashAttention-3.

## Abstract

Efficient inference of Multi-Head Latent Attention (MLA) is challenged by deploying the DeepSeek-R1 671B model on a single Multi-GPU server. This paper introduces FlashMLA-ETAP, a novel framework that enhances MLA inference for the single-instance deployment scenario on NVIDIA H20 GPUs. We propose the Efficient Transpose Attention Pipeline (ETAP), which reconfigures attention computation through transposition to align the KV context length with the \(M\)-dimension in WGMMA operations, significantly reducing redundant computations. FlashMLA-ETAP achieves a 2.78x speedup over FlashMLA at 64K sequence length (batch size 16), with 5.24x and 4.94x improvements over FlashAttention-3 and FlashInfer, respectively, while maintaining numerical stability with a 15.2x lower RMSE (\(1.25 \times 10^{-5}\)) than FlashAttention-3. Furthermore, ETAP's design enables seamless integration into frameworks like FlashAttention-3 and FlashInfer, supported by a detailed theoretical analysis. Our work addresses a critical gap in resource-constrained inference, offering a scalable solution for mid-tier GPUs and paving the way for broader adoption in hardware-aware optimization. Code is available at https://github.com/pengcuo/FlashMLA-ETAP.

## Contributions (from the paper)

In summary, this paper makes the following key contributions: We introduce the Efficient Transpose Attention Pipeline (ETAP), a novel computation mode that leverages transposition to reduce redundant calculations, tailored to the architectural constraints of mid-tier GPUs like the H20. We develop FlashMLA-ETAP, enhancing FlashMLA with ETAP to enable efficient MLA inference on mid-tier GPUs, with ETAP designed to be easily integrated into other attention frameworks such as FlashInfer and FlashAttention-3. Our experiments show that the FlashMLA-ETAP framework achieves impressive performance on the NVIDIA H20 GPU, delivering up to a 2.78x speedup over the baseline FlashMLA framework.

## Compute & hardware (extracted from full text)

Hardware setup mentions:

- Our experiments show that the FlashMLA-ETAP framework achieves impressive performance on the NVIDIA H20 GPU, delivering up to a 2.78x speedup over the baseline FlashMLA framework.
- The original computation mode poses significant challenges on the NVIDIA H20 GPU, primarily due to the mismatch between the workload and the GPU’s architectural constraints.
- To evaluate the performance of FlashMLA-ETAP, we conducted experiments comparing its inference throughput against FlashAttention-3, FlashInfer, and FlashMLA using the DeepSeek-R1 model on an NVIDIA H20 GPU.
- We computed the RMSE between the FP16 outputs of each framework and a double-precision (FP64) reference implementation of the attention mechanism, using the DeepSeek-R1 model across a representative set of sequence lengths and batch sizes on the H20 GPU.

## Main results tables (from the paper)

**Table 1: RMSE comparison between FlashAttention-3 and FlashMLA-ETAP in FP16 precision.**

| Framework | RMSE |
| --- | --- |
| FlashAttention-3 | $1.9\times 10^{-4}$ |
| FlashMLA-ETAP | $1.25\times 10^{-5}$ |

## Conclusion (from the paper)

This paper introduces FlashMLA-ETAP, a novel framework that significantly enhances Multi-Head Latent Attention (MLA) inference on the NVIDIA H20 GPU, constrained by its 148 TFLOPS FP16 compute capability. By proposing the Efficient Transpose Attention Pipeline (ETAP), which reconfigures attention computation to reduce padding overhead through transposition, FlashMLA-ETAP achieves a 2.78x speedup over FlashMLA at 64K sequence length (batch size 16), outperforming FlashAttention-3 by 5.24x and FlashInfer by 4.94x, as demonstrated in our experiments. ETAP’s design also proves theoretically compatible with FlashAttention-3 and FlashInfer, highlighting its potential for broader adoption in hardware-aware inference optimization. Additionally, FlashMLA-ETAP exhibits a 15.2x lower RMSE (1.25\times 10^{-5}) compared to FlashAttention-3, ensuring numerical stability in long-context scenarios. However, the evaluation is limited to the H20, autoregressive decoding with one token per forward pass, and specific configurations (16 heads, 576 head dimension, up to 64K sequences), potentially restricting generalizability across diverse GPUs, inference paradigms, or larger models. Future work should focus on empirically validating ETAP’s integration into FlashAttention-3 and FlashInfer, optimizing for other Hopper-based mid-tier GPUs, and exploring broader sequence lengths and configurations to …

## Limitations (from the paper)

This paper introduces FlashMLA-ETAP, a novel framework that significantly enhances Multi-Head Latent Attention (MLA) inference on the NVIDIA H20 GPU, constrained by its 148 TFLOPS FP16 compute capability. By proposing the Efficient Transpose Attention Pipeline (ETAP), which reconfigures attention computation to reduce padding overhead through transposition, FlashMLA-ETAP achieves a 2.78x speedup over FlashMLA at 64K sequence length (batch size 16), outperforming FlashAttention-3 by 5.24x and FlashInfer by 4.94x, as demonstrated in our experiments. ETAP’s design also proves theoretically compatible with FlashAttention-3 and FlashInfer, highlighting its potential for broader adoption in hardware-aware inference optimization. Additionally, FlashMLA-ETAP exhibits a 15.2x lower RMSE (1.25\times 10^{-5}) compared to FlashAttention-3, ensuring numerical stability in long-context scenarios. …

<details><summary>Paper outline</summary>

- 1 Introduction
- 2 Related Work
- 3 Methodology
- 3.1 Efficient Transpose Attention Pipeline (ETAP)
- 3.2 FlashMLA-ETAP Algorithm
- 4 Experimental Evaluation
- 4.1 Experimental Setup
- 4.2 Results and Analysis
- 4.3 Numerical Error Validation
- 4.4 Discussion
- 5 Conclusion, Limitations, Future Work

</details>

## Reproduce / read

```sh
arxiv read 2506.01969            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2506.01969 "method"
arxiv bibtex 2506.01969
```
