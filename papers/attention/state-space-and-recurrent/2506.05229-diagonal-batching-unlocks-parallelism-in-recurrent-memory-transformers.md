# Diagonal Batching Unlocks Parallelism in Recurrent Memory Transformers for Long Contexts

**arXiv:** [2506.05229](https://arxiv.org/abs/2506.05229) · [PDF](https://arxiv.org/pdf/2506.05229) · [HTML](https://arxiv.org/html/2506.05229) · **First submitted:** 2025-06-05  
**Category:** [State-space models & recurrent LMs (Mamba, RWKV, xLSTM)](README.md) — **rank #20 of 93** by impact score  
**Also relevant to:** [Request scheduling, batching & SLO serving](../../serving-systems/scheduling-and-batching/README.md)  

> **TL;DR** — Diagonal Batching is introduced, a scheduling scheme that unlocks parallelism across segments in RMTs while preserving exact recurrence, enabling efficient GPU inference even for single long-context inputs without complex batching and pipelining techniques.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Danil Sivtsov, Ivan Rodkin, Gleb Kuzmin, Yuri Kuratov, Ivan Oseledets |
| arXiv categories | cs.LG, cs.CL |
| Citations (Semantic Scholar, 2026-09-25) | 2 (0 influential) |
| Hugging Face upvotes | 37 |
| Code | https://github.com/svtdanny/diagonal-batching |
| Training requirement (from abstract) | training-free / post-training |
| Models used | LLaMA-1 |
| Impact score | 5.06 |

## Key results (claimed in abstract)

- Applied to a LLaMA-1B ARMT model, Diagonal Batching yields a 3.3x speedup over standard full-attention LLaMA-1B and a 1.8x speedup over the sequential RMT implementation on 131,072-token sequences.

## Abstract

Transformer models struggle with long-context inference due to their quadratic time and linear memory complexity. Recurrent Memory Transformers (RMTs) offer a solution by reducing the asymptotic cost to linear time and constant memory usage. However, their memory update mechanism leads to sequential execution, causing a performance bottleneck. We introduce Diagonal Batching, a scheduling scheme that unlocks parallelism across segments in RMTs while preserving exact recurrence. This approach eliminates the sequential constraint, enabling efficient GPU inference even for single long-context inputs without complex batching and pipelining techniques. Because the technique is purely a run-time computation reordering, existing RMT models adopt it with no retraining. Applied to a LLaMA-1B ARMT model, Diagonal Batching yields a 3.3x speedup over standard full-attention LLaMA-1B and a 1.8x speedup over the sequential RMT implementation on 131,072-token sequences. By removing sequential bottleneck, Diagonal Batching reduces inference cost and latency, thereby strengthening RMTs as a practical solution for real-world, long-context applications.

**Keywords:** Transformer models, long-context inference, quadratic time complexity, linear memory complexity, Recurrent Memory Transformers, RMTs, memory update mechanism, sequential execution, Diagonal Batching, run-time computation reordering, parallelism, GPU inference

## Reproduce / read

```sh
arxiv read 2506.05229            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2506.05229 "method"
arxiv bibtex 2506.05229
```
