# SpotAttention: Plug-In Block-Sparse Routing for Pretrained Long-Context Transformers

**arXiv:** [2606.22874](https://arxiv.org/abs/2606.22874) · [PDF](https://arxiv.org/pdf/2606.22874) · [HTML](https://arxiv.org/html/2606.22874) · **First submitted:** 2026-06-22  
**Category:** [Sparse attention (trainable & training-free)](README.md) — **rank #60 of 180** by impact score  
**Also relevant to:** [Long context, context extension & positional encoding](../long-context-and-position/README.md) · [MoE architecture & routing](../../mixture-of-experts/architecture-and-routing/README.md) · [Pre-training recipes & efficiency](../../training/pretraining-recipes-and-efficiency/README.md)  

## At a glance

| Field | Value |
| --- | --- |
| Authors | Huzama Ahmad, Se-Young Yun |
| arXiv categories | cs.LG, cs.AI |
| arXiv comments | 24 pages, 10 figures, 9 tables |
| Citations (Semantic Scholar, 2026-09-25) | 4 (2 influential) |
| Hugging Face upvotes | 0 |
| Code | — |
| Training requirement (from abstract) | training-free / post-training; involves (re)training / fine-tuning |
| Models used | Qwen3, Qwen3.5 |
| Impact score | 3.29 |

## Key results (claimed in abstract)

- Across Qwen3 (dense, 4B-32B) and Qwen3.5 (hybrid linear/full attention, 4B-9B), SpotAttention matches dense accuracy at contexts up to 128K tokens, eight times the training length.
- Decode at L=128K runs 3.9x faster than FlashAttention and 1.8x faster than Twilight, the strongest training-free baseline.
- Quantizing the selector's K-cache to INT4 or FP4 microscale shrinks it 3.5x at no accuracy cost.

## Abstract

Long contexts have become standard in pretrained LLMs, yet they remain expensive to run: prefill compute grows quadratically with sequence length, and every decode step re-reads a key-value cache that grows linearly with it. Sparse attention cuts these costs by attending only to a relevant subset of past tokens, but selecting that subset is itself expensive. We present SpotAttention, a lightweight selector that attaches to a frozen pretrained transformer and learns by KL distillation to estimate its attention distribution. The selector picks the top-K keys each query attends to, and because its estimate is a calibrated distribution, a dual top-p rule reads the per-query, per-layer budget directly from it. Across Qwen3 (dense, 4B-32B) and Qwen3.5 (hybrid linear/full attention, 4B-9B), SpotAttention matches dense accuracy at contexts up to 128K tokens, eight times the training length. Decode at L=128K runs 3.9x faster than FlashAttention and 1.8x faster than Twilight, the strongest training-free baseline. Quantizing the selector's K-cache to INT4 or FP4 microscale shrinks it 3.5x at no accuracy cost.

## Reproduce / read

```sh
arxiv read 2606.22874            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2606.22874 "method"
arxiv bibtex 2606.22874
```
