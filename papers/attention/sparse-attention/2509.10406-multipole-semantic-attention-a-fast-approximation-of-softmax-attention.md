# Multipole Semantic Attention: A Fast Approximation of Softmax Attention for Pretraining

**arXiv:** [2509.10406](https://arxiv.org/abs/2509.10406) · [PDF](https://arxiv.org/pdf/2509.10406) · [HTML](https://arxiv.org/html/2509.10406) · **First submitted:** 2026-06-30  
**Category:** [Sparse attention (trainable & training-free)](README.md) — **rank #119 of 181** by impact score  

> **TL;DR** — Multipole Semantic Attention (MuSe) is presented, which accelerates 64k-context pretraining by 36% while matching baseline loss, requiring no architectural changes and enabling drop-in compatibility with existing pretrained models.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Rupert Mitchell, Kristian Kersting |
| arXiv categories | cs.LG |
| Citations (Semantic Scholar, 2026-09-25) | 1 (0 influential) |
| Hugging Face upvotes | 0 |
| Code | — |
| Training requirement (from abstract) | training-free / post-training; involves (re)training / fine-tuning |
| Models used | Llama-3.1-8B |
| Impact score | 0.82 |

## Key results (claimed in abstract)

- We present Multipole Semantic Attention (MuSe), which accelerates 64k-context pretraining by 36% while matching baseline loss, requiring no architectural changes.
- This yields query-specific summaries that substantially outperform spatial blocking at matched sparsity, while also enabling drop-in compatibility with existing pretrained models; we validate on Llama 3.1-8B and 3.2-1B without retraining.

## Abstract

Pretraining transformers on long sequences (entire code repositories, collections of related documents) is bottlenecked by quadratic attention costs. We present Multipole Semantic Attention (MuSe), which accelerates 64k-context pretraining by 36% while matching baseline loss, requiring no architectural changes. MuSe clusters queries and keys separately in representation space. This yields query-specific summaries that substantially outperform spatial blocking at matched sparsity, while also enabling drop-in compatibility with existing pretrained models; we validate on Llama 3.1-8B and 3.2-1B without retraining. We pretrain language models up to 1B parameters at 64k context on code and scientific documents, confirming that MuSe preserves quality and long-context utilization during training.

## Reproduce / read

```sh
arxiv read 2509.10406            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2509.10406 "method"
arxiv bibtex 2509.10406
```
