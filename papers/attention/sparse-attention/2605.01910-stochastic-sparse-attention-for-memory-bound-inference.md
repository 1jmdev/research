# Stochastic Sparse Attention for Memory-Bound Inference

**arXiv:** [2605.01910](https://arxiv.org/abs/2605.01910) · [PDF](https://arxiv.org/pdf/2605.01910) · [HTML](https://arxiv.org/html/2605.01910) · **First submitted:** 2026-06-03  
**Category:** [Sparse attention (trainable & training-free)](README.md) — **rank #99 of 180** by impact score  
**Also relevant to:** [Memory management & offloading (weights, activations)](../../serving-systems/memory-and-offloading/README.md) · [Attention kernels & IO-aware implementations](../kernels-and-io-aware/README.md) · [GPU kernels, compilers & operator optimization](../../serving-systems/gpu-kernels-and-compilers/README.md) · [Long context, context extension & positional encoding](../long-context-and-position/README.md) · [KV cache — general, systems & analysis](../../kv-cache/_general/README.md)  

> **TL;DR** — This work presents Stochastic Additive No-mulT Attention (SANTA), a method that sparsifies value-cache access by sampling indices from the post-softmax distribution and aggregates only those value rows, and proposes Bernoulli's sampling as a complementary technique to sparsify the score stage, reducing key-feature access through stochastic ternary queries.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Kyle Lee, Corentin Delacour, Kevin Callahan-Coray, Kyle Jiang, Can Yaras, Samet Oymak, Tathagata Srimani, Kerem Y. Camsari |
| arXiv categories | cs.LG, cs.AI, cs.DC |
| Venue | ICML 2026 |
| arXiv comments | Code available at https://github.com/OPUSLab/SANTA |
| Citations (Semantic Scholar, 2026-09-25) | 0 (0 influential) |
| Hugging Face upvotes | 0 |
| Code | https://github.com/OPUSLab/SANTA.git |
| Models used | Llama-3.1-8B |
| Hardware | RTX 6000 |
| Impact score | 1.2 |

## Key results (claimed in abstract)

- Evaluated on Llama-3.1-8B-Instruct at 32k-token contexts, S$^2$ANTA matches baseline accuracy while achieving up to $1.5\times$ decode-step attention-kernel speedup over FlashInfer and FlashDecoding on an NVIDIA RTX 6000 Ada.
- In batched long-context generation, these kernel gains translate to up to $1.25\times$ end-to-end decode-latency speedup.

## Abstract

Autoregressive decoding becomes bandwidth-limited at long contexts, as generating each token requires reading all $n_k$ key and value vectors from KV cache. We present Stochastic Additive No-mulT Attention (SANTA), a method that sparsifies value-cache access by sampling $S \ll n_k$ indices from the post-softmax distribution and aggregates only those value rows. This yields an unbiased estimator of the post-softmax value aggregation while replacing value-stage multiply-accumulates with gather-and-add. We introduce stratified and systematic sampling to design variance-reduced, GPU-friendly variants. Evaluated on Llama-3.1-8B-Instruct at 32k-token contexts, S$^2$ANTA matches baseline accuracy while achieving up to $1.5\times$ decode-step attention-kernel speedup over FlashInfer and FlashDecoding on an NVIDIA RTX 6000 Ada. In batched long-context generation, these kernel gains translate to up to $1.25\times$ end-to-end decode-latency speedup. Finally, we propose Bernoulli $qK^\mathsf{T}$ sampling as a complementary technique to sparsify the score stage, reducing key-feature access through stochastic ternary queries. Both methods are complementary to upstream quantization, low-rank projection, KV-cache compression, and KV-cache selection methods. Together, they point toward sparse, multiplier-free, and energy-efficient inference. We open-source our kernels at: https://github.com/OPUSLab/SANTA.git

## Reproduce / read

```sh
arxiv read 2605.01910            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2605.01910 "method"
arxiv bibtex 2605.01910
```
