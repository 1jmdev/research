# CompactAttention: Accelerating Chunked Prefill with Block-Union KV Selection

**arXiv:** [2605.16839](https://arxiv.org/abs/2605.16839) · [PDF](https://arxiv.org/pdf/2605.16839) · [HTML](https://arxiv.org/html/2605.16839) · **First submitted:** 2026-05-16  
**Category:** [Sparse attention (trainable & training-free)](README.md) — **rank #59 of 180** by impact score  
**Also relevant to:** [GPU kernels, compilers & operator optimization](../../serving-systems/gpu-kernels-and-compilers/README.md) · [KV cache — general, systems & analysis](../../kv-cache/_general/README.md)  

> **TL;DR** — CompactAttention, a chunked-prefill attention mechanism based on Block-Union KV Selection, which treats 2D block-sparse masks as KV-selection signals rather than direct sparse-kernel execution plans, and converts them into GQA-aware per-group KV block tables through Q-block union and intra-group union.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Jiwon Song, Dongwon Jo, Beomseok Kang, Jae-Joon Kim |
| Organisation (HF) | Seoul National University VLSI Lab |
| arXiv categories | cs.CL |
| Citations (Semantic Scholar, 2026-09-25) | 0 (0 influential) |
| Hugging Face upvotes | 12 |
| Code | https://github.com/jiwonsong-dev/CompactAttention |
| Models used | LLaMA-3.1-8B |
| Impact score | 3.37 |

## Key results (claimed in abstract)

- On LLaMA-3.1-8B-Instruct, CompactAttention maintains accuracy close to dense attention on the RULER benchmark while delivering up to 2.72$\times$ attention speedup at 128K context length under chunked prefill.

## Abstract

Chunked prefill has become a widely adopted serving strategy for long-context large language models, but efficient attention computation in this regime remains challenging. Existing sparse attention methods are primarily designed for one-shot prefill and do not translate efficiently to chunked prefill: block-sparse kernels lose efficiency when the query length is limited by the chunk size, while fine-grained pattern search becomes costly when repeated over the accumulated KV cache at every chunk. QUOKA, a recent method that directly targets chunked prefill, avoids sparse-kernel overhead but relies on query-subsampled, token-level KV selection, which can miss query-specific KV entries and introduce explicit KV-copy overhead. To address these limitations, we propose CompactAttention, a chunked-prefill attention mechanism based on Block-Union KV Selection. CompactAttention treats 2D block-sparse masks as KV-selection signals rather than direct sparse-kernel execution plans, and converts them into GQA-aware per-group KV block tables through Q-block union and intra-group union. This construction produces the minimal block tables that preserve all KV blocks selected by the input masks under paged execution constraints, enabling selected KV blocks to be accessed in place without explicit KV compaction. On LLaMA-3.1-8B-Instruct, CompactAttention maintains accuracy close to dense attention on the RULER benchmark while delivering up to 2.72$\times$ attention speedup at 128K context length under chunked prefill.

**Keywords:** chunked prefill, sparse attention, block-sparse kernels, KV selection, Block-Union KV Selection, GQA-aware, paged execution, attention speedup, RULER benchmark

## Reproduce / read

```sh
arxiv read 2605.16839            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2605.16839 "method"
arxiv bibtex 2605.16839
```
