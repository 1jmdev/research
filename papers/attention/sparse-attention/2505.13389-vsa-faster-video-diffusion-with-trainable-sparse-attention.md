# VSA: Faster Video Diffusion with Trainable Sparse Attention

**arXiv:** [2505.13389](https://arxiv.org/abs/2505.13389) · [PDF](https://arxiv.org/pdf/2505.13389) · [HTML](https://arxiv.org/html/2505.13389) · **First submitted:** 2025-10-28  
**Category:** [Sparse attention (trainable & training-free)](README.md) — **rank #4 of 181** by impact score  

> **TL;DR** — VSA is established, a trainable, hardware-efficient sparse attention that replaces full attention at \emph{both} training and inference, and a key enabler for further scaling of video diffusion models.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Peiyuan Zhang, Yongqi Chen, Haofeng Huang, Will Lin, Zhengzhong Liu, Ion Stoica, Eric Xing, Hao Zhang |
| arXiv categories | cs.CV |
| Venue | Accepted by Neurips 2025 |
| arXiv comments | Accepted by Neurips 2025 |
| Citations (Semantic Scholar, 2026-09-25) | 102 (10 influential) |
| Hugging Face upvotes | 38 |
| Code | https://github.com/hao-ai-lab/FastVideo |
| Training requirement (from abstract) | involves (re)training / fine-tuning |
| Hardware | H200 (×7 mentions), H100 (×1 mentions) |
| **Compute (est. H100-hours)** | **0.00 – 99k** (auto-extracted, see below) |
| Largest token count mentioned | 1M tokens |
| Impact score | 15.56 |

## Abstract

Scaling video diffusion transformers (DiTs) is limited by their quadratic 3D attention, even though most of the attention mass concentrates on a small subset of positions. We turn this observation into VSA, a trainable, hardware-efficient sparse attention that replaces full attention at \emph{both} training and inference. In VSA, a lightweight coarse stage pools tokens into tiles and identifies high-weight \emph{critical tokens}; a fine stage computes token-level attention only inside those tiles subjecting to block computing layout to ensure hard efficiency. This leads to a single differentiable kernel that trains end-to-end, requires no post-hoc profiling, and sustains 85\% of FlashAttention3 MFU. We perform a large sweep of ablation studies and scaling-law experiments by pretraining DiTs from 60M to 1.4B parameters. VSA reaches a Pareto point that cuts training FLOPS by 2.53$\times$ with no drop in diffusion loss. Retrofitting the open-source Wan-2.1 model speeds up attention time by 6$\times$ and lowers end-to-end generation time from 31s to 18s with comparable quality. These results establish trainable sparse attention as a practical alternative to full attention and a key enabler for further scaling of video diffusion models. Code will be available at https://github.com/hao-ai-lab/FastVideo.

## Compute & hardware (extracted from full text)

Estimates are normalised to H100-hours with the factors in [METHODOLOGY.md](../../../METHODOLOGY.md). Always check the quoted sentence: the parser can't tell which experiment a number belongs to.

| Est. H100-h | Parsed | Source sentence |
| --- | --- | --- |
| 99k | 9e+04 H200 GPU-h | To the best of our knowledge, VSA is the first trainable sparse attention approach that, based on extensive experiments totaling around 90k H200 hours, shows better scaling than full attention on DiTs. |
| 704 | 64× H200 × 10 h | Each ablation job takes around 10 hours on 64 Nvidia H200 GPU. |
| 845 | 64× H200 × 12 h | Our Sparse Distillation on Wan-1.3B uses 64 H200 with a per-GPU batch size of 1, and runs for 12 hours. |
| 0.00 | 1× H200 × 0.00139 h | After 4,000 steps training, the 3-step generator achieves a 50.9× reduction in denoising time relative to the baseline model and can generate a 5-second video in approximately 5 seconds on a single H200. |

Hardware setup mentions:

- Each model was trained with compute budgets up to 4\times 10^{21} FLOPS on 128 H200 GPUs with sequence length of 16K.
- Training was conducted on 32 H200 GPUs using DDP as the parallelism strategy.
- Training was conducted on 64 H200 GPUs using DDP as the parallelism strategy.

## Main results tables (from the paper)

**(a) VSA v.s. other attention.**

| Exp ID | Case | Loss – Opt | Loss – Over |
| --- | --- | --- | --- |
| 1 | Compress KV | 0.15281 | 0.14282 |
| 2 | Spatial Temporal | 0.13574 | 0.13034 |
| 3 | Spatial Full | 0.13555 | 0.12811 |
| 4 | Strided Window | 0.13271 | 0.12716 |
| 5 | Full | 0.13877 | 0.12703 |
| 6 | VSA | 0.13162 | 0.12687 |
| Exp ID | Case | Loss |
| --- | --- | --- |
| 7 | L | 0.13330 |
| 8 | F (no $\mathbf{O_{c}}$) | 0.13296 |
| 9 | C & L | 0.13220 |
| 10 | C & F | 0.13162 |
| 11 | C & F & L | 0.13194 |
| 12 | C & F & L & E | 0.13124 |
| 13 | C & (F + L) | 0.13192 |
| $B$ | $(C_{t},\ C_{h},\ C_{w})$ |
| … |

**Table 2: Model configuration and training hyperparameters used for the ablation studies.**

| Model Config | Value |
| --- | --- |
| Head Dim | 64 |
| FFN Dim | 3072 |
| Cross Attn Norm | True |
| Freq Dim | 256 |
| In Channels | 16 |
| Out Channels | 16 |
| Num Heads | 12 |
| Num Layers | 12 |
| Patch Size | $1\times 2\times 2$ |
| QK Norm | RMS Norm (across heads) |
| Epsilon | $1\times 10^{-6}$ |
| Text Dim | 4096 |
| Hyperparameter | Value |
| --- | --- |
| Learning Rate | $6\times 10^{-4}$ |
| LR Scheduler | Constant |
| … |

## Limitations (from the paper)

We present VSA, a trainable and hardware-efficient sparse attention tailored for scaling DiTs. Unlike prior work that applies sparsity post-hoc, VSA jointly learns to predict and apply attention sparsity at training and remains compatible with block-sparse compute layout. VSA currently operates with a fixed cube size of (4,4,4), which requires video latent dimensions to be divisible by 4. While this may restrict the set of compatible resolutions, it can be addressed in practice by generating a slightly larger latent and cropping to the target shape. Another open question is how to determine the optimal sparsity level. While our scaling experiments (§2) provide preliminary insights, a complete understanding may require extending scaling laws to explicitly account for sparsity, alongside model size and training compute. Across diverse model sizes (60M to 1.4B) and budgets (up to 4\times …

<details><summary>Paper outline</summary>

- 1 Introduction
- 2 Methods
- 2.1 Sparse Attention Design Space
- 2.2 VSA: Video Sparse Attention
- 2.3 Sparse Adaptation & Distillation
- 2.4 Kernel Implementation
- 3 Experiments
- 3.1 Ablation Studies
- 3.2 Scaling Studies
- 3.3 Sparse Adaptation & Distillation
- 3.4 Kernel Performance
- 3.5 Inspecting VSA
- 4 Related Work
- 5 Limitation and Conclusion
- Acknowledgments
- Appendix A Qualitative Examples
- Appendix B Pseudocode of VSA
- Appendix C Experimental Details
- C.1 Model Architecture
- C.2 Ablation Experiments Setup
- C.3 Baseline Attention Variants
- C.4 FLOPS Calculation
- C.5 Sparse Adaptation Setup
- C.6 Sparse Distillation Setup
- Appendix D Coarse Stage Runtime
- Appendix E Discussion and Extended Related Work
- Appendix F Broader Impact

</details>

**Keywords:** video diffusion transformers, DiTs, quadratic 3D attention, VSA, token-level attention, block computing, FlashAttention3, MFU, FLOPS, diffusion loss, Wan-2.1 model

## Reproduce / read

```sh
arxiv read 2505.13389            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2505.13389 "method"
arxiv bibtex 2505.13389
```
