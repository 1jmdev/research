# HieraSparse: Hierarchical Semi-Structured Sparse KV Attention

**arXiv:** [2604.16864](https://arxiv.org/abs/2604.16864) · [PDF](https://arxiv.org/pdf/2604.16864) · [HTML](https://arxiv.org/html/2604.16864) · **First submitted:** 2026-08-17  
**Category:** [Sparse attention (trainable & training-free)](README.md) — **rank #130 of 181** by impact score  
**Also relevant to:** [KV cache eviction / token selection / sparse retrieval](../../kv-cache/eviction-and-token-selection/README.md) · [GPU kernels, compilers & operator optimization](../../serving-systems/gpu-kernels-and-compilers/README.md) · [KV cache — general, systems & analysis](../../kv-cache/_general/README.md)  

> **TL;DR** — A hierarchical KV Cache compression framework with acceleration kernels that leverage GPU sparse tensor cores to speed up semi-structured KV Cache attention for both the prefill and decode phases and evaluates the generation quality of \textit{HieraSparse} with a simple magnitude-based pruning method.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Haoxuan Wang, Chen Wang |
| arXiv categories | cs.DC, cs.AR |
| Citations (Semantic Scholar, 2026-09-25) | 0 (0 influential) |
| Hugging Face upvotes | 0 |
| Code | https://github.com/psl-ntu/HieraSparse |
| Impact score | 0.5 |

## Key results (claimed in abstract)

- Compared to the state-of-the-art decode method that utilizes unstructured sparsity, \textit{HieraSparse} achieves $\mathbf{1.2\times}$ KV compression ratio and $\mathbf{4.57\times}$ attention speedup at the same sparsity level.
- Furthermore, we extended the semi-structured KV Cache pruning to the prefill stage, which demonstrated up to $\mathbf{1.85\times}$ attention speedup at the highest sparsity.
- Lastly, we evaluate the generation quality of \textit{HieraSparse} with a simple magnitude-based pruning method, and the results show that $\mathbf{1.34\times}$ prefill and $\mathbf{1.71\times}$ decode attention speedup can be achieved without significant quality drop.

## Abstract

The deployment of long-context Large Language Models (LLMs) poses significant challenges due to the intense computational cost of self-attention and the substantial memory overhead of the Key-Value Cache (KV Cache). In this paper, we introduce \textit{HieraSparse}, a hierarchical KV Cache compression framework with acceleration kernels that leverage GPU sparse tensor cores to speed up semi-structured KV Cache attention for both the prefill and decode phases. With the hierarchical design, our method allows for a flexible quality-sparsity trade-off and successfully converts sparsity into efficiency. Compared to the state-of-the-art decode method that utilizes unstructured sparsity, \textit{HieraSparse} achieves $\mathbf{1.2\times}$ KV compression ratio and $\mathbf{4.57\times}$ attention speedup at the same sparsity level. Furthermore, we extended the semi-structured KV Cache pruning to the prefill stage, which demonstrated up to $\mathbf{1.85\times}$ attention speedup at the highest sparsity. Lastly, we evaluate the generation quality of \textit{HieraSparse} with a simple magnitude-based pruning method, and the results show that $\mathbf{1.34\times}$ prefill and $\mathbf{1.71\times}$ decode attention speedup can be achieved without significant quality drop. The codebase can be found at https://github.com/psl-ntu/HieraSparse.

## Reproduce / read

```sh
arxiv read 2604.16864            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2604.16864 "method"
arxiv bibtex 2604.16864
```
