# Learning how to Forget: Fine-tuning for Long-Context Sparse Attention

**arXiv:** [2608.19920](https://arxiv.org/abs/2608.19920) · [PDF](https://arxiv.org/pdf/2608.19920) · [HTML](https://arxiv.org/html/2608.19920) · **First submitted:** 2026-08-20  
**Category:** [Sparse attention (trainable & training-free)](README.md) — **rank #132 of 180** by impact score  
**Also relevant to:** [Long context, context extension & positional encoding](../long-context-and-position/README.md) · [KV cache eviction / token selection / sparse retrieval](../../kv-cache/eviction-and-token-selection/README.md)  

> **TL;DR** — This work provides a new method for fine-tuning models with sparse attention that works for any KV cache policy, runs on a moderate hardware budget, and allows the model to co-adapt with the policy, often outperforming models trained with exact attention (sequence parallelism).

## At a glance

| Field | Value |
| --- | --- |
| Authors | Matthias Seeger, Zeyu Zhang, Vihang Patil, Konstantinos Benidis, Sebastian Schelter |
| arXiv categories | cs.CL |
| arXiv comments | 39 pages, no figures |
| Citations (Semantic Scholar, 2026-09-25) | 0 (0 influential) |
| Hugging Face upvotes | 0 |
| Code | https://github.com/awslabs/keys_values |
| Training requirement (from abstract) | involves (re)training / fine-tuning |
| Hardware | A100 |
| Impact score | 0.5 |

## Key results (claimed in abstract)

- It works for any KV cache policy, runs on a moderate hardware budget (e.g., a single Nvidia A100 GPU with 40 GB RAM), and allows the model to co-adapt with the policy, often outperforming models trained with exact attention (sequence parallelism).

## Abstract

A lot of prior work addressed key-value (KV) cache selection and compression by sparse attention to enable long-context inference for transformer language models without excessive hardware budgets. We provide a new method for fine-tuning models with sparse attention. It works for any KV cache policy, runs on a moderate hardware budget (e.g., a single Nvidia A100 GPU with 40 GB RAM), and allows the model to co-adapt with the policy, often outperforming models trained with exact attention (sequence parallelism). We also provide an efficient implementation of H2O sparse attention (the leading policy in our experiments) with dedicated scaled dot product attention kernel support. KeysAndValues (https://github.com/awslabs/keys_values), a new open source library for long-context inference and fine-tuning, provides easy-to-use and performant code for all methods discussed here.

## Reproduce / read

```sh
arxiv read 2608.19920            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2608.19920 "method"
arxiv bibtex 2608.19920
```
