# NOSA: Native and Offloadable Sparse Attention

**arXiv:** [2510.13602](https://arxiv.org/abs/2510.13602) · [PDF](https://arxiv.org/pdf/2510.13602) · [HTML](https://arxiv.org/html/2510.13602) · **First submitted:** 2026-01-29  
**Category:** [Sparse attention (trainable & training-free)](README.md) — **rank #43 of 181** by impact score  
**Also relevant to:** [KV cache offloading & hierarchical storage](../../kv-cache/offloading-and-hierarchical-storage/README.md) · [Memory management & offloading (weights, activations)](../../serving-systems/memory-and-offloading/README.md) · [KV cache — general, systems & analysis](../../kv-cache/_general/README.md)  

> **TL;DR** — This work proposes NOSA, a trainable sparse attention mechanism natively designed for KV cache offloading that explicitly constrains the volume of CPU-GPU KV transfers, thereby achieving low communication overhead and high decoding throughput.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Yuxiang Huang, Pengjie Wang, Jicheng Han, Weilin Zhao, Zhou Su, Ao Sun, Hongya Lyu, Hengyu Zhao, Yudong Wang, Chaojun Xiao, … (+2) |
| arXiv categories | cs.CL, cs.AI, cs.LG |
| arXiv comments | Preprint |
| Citations (Semantic Scholar, 2026-09-25) | 4 (1 influential) |
| Hugging Face upvotes | 7 |
| Code | https://github.com/thunlp/NOSA |
| Training requirement (from abstract) | training-free / post-training |
| Impact score | 4.84 |

## Key results (claimed in abstract)

- Empirical results on 1,3,8B LLMs demonstrate that NOSA outperforms KV cache offloading baselines on general, long-input, and long-generation tasks, while boosting decoding throughput by up to 5.04x, 1.92x, and 1.83x over FullAttn, InfLLMv2, and ShadowKV, respectively.

## Abstract

Decoding throughput improvements from larger inference batches are limited by GPU memory, which is largely consumed by the key-value (KV) cache. Prior training-free KV cache offloading alleviates this by keeping redundant context on the CPU and fetching only a sparse subset for attention, but it often degrades long-generation quality due to training-inference mismatch on sparse patterns. Meanwhile, trainable sparse attention is incompatible with efficient offloading, as unconstrained KV accesses may force large CPU-to-GPU transfers and erase throughput gains. To this end, we propose NOSA, a trainable sparse attention mechanism natively designed for KV cache offloading. NOSA explicitly constrains the volume of CPU-GPU KV transfers, thereby achieving low communication overhead and high decoding throughput. We further build NOSI, a KV cache offloading inference system that fully unlocks NOSA's efficiency. Empirical results on 1,3,8B LLMs demonstrate that NOSA outperforms KV cache offloading baselines on general, long-input, and long-generation tasks, while boosting decoding throughput by up to 5.04x, 1.92x, and 1.83x over FullAttn, InfLLMv2, and ShadowKV, respectively. We release our code at https://github.com/thunlp/NOSA.

**Keywords:** sparse attention, key-value cache, token selection, query-aware, query-agnostic, NOSA, decoding throughput, pretrain, InfLLM-V2

## Reproduce / read

```sh
arxiv read 2510.13602            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2510.13602 "method"
arxiv bibtex 2510.13602
```
