# HeadInfer: Memory-Efficient LLM Inference by Head-wise Offloading

**arXiv:** [2502.12574](https://arxiv.org/abs/2502.12574) · [PDF](https://arxiv.org/pdf/2502.12574) · [HTML](https://arxiv.org/html/2502.12574) · **First submitted:** 2025-02-18  
**Category:** [Memory management & offloading (weights, activations)](README.md) — **rank #1 of 39** by impact score  
**Also relevant to:** [Long context, context extension & positional encoding](../../attention/long-context-and-position/README.md) · [KV cache — general, systems & analysis](../../kv-cache/_general/README.md)  

> **TL;DR** — This paper proposes HEADINFER, which offloads the KV cache to CPU RAM while avoiding the need to fully store the KV cache for any transformer layer on the GPU, and evaluates it on the Llama-3-8B model with a 1-million-token sequence.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Cheng Luo, Zefan Cai, Hanshi Sun, Jinqi Xiao, Bo Yuan, Wen Xiao, Junjie Hu, Jiawei Zhao, Beidi Chen, Anima Anandkumar |
| arXiv categories | cs.LG, cs.AI |
| Citations (Semantic Scholar, 2026-09-25) | 14 (0 influential) |
| Hugging Face upvotes | 13 |
| Code | https://github.com/wdlctc/headinfer |
| Models used | Llama-3-8B |
| Hardware | RTX 4090 |
| Impact score | 5.73 |

## Key results (claimed in abstract)

- We evaluate HEADINFER on the Llama-3-8B model with a 1-million-token sequence, reducing the GPU memory footprint of the KV cache from 128 GB to 1 GB and the total GPU memory usage from 207 GB to 17 GB, achieving a 92% reduction compared to BF16 baseline inference.
- Notably, HEADINFER enables 4-million-token inference with an 8B model on a single consumer GPU with 24GB memory (e.g., NVIDIA RTX 4090) without approximation methods.

## Abstract

Transformer-based large language models (LLMs) demonstrate impressive performance in long context generation. Extending the context length has disproportionately shifted the memory footprint of LLMs during inference to the key-value cache (KV cache). In this paper, we propose HEADINFER, which offloads the KV cache to CPU RAM while avoiding the need to fully store the KV cache for any transformer layer on the GPU. HEADINFER employs a fine-grained, head-wise offloading strategy, maintaining only selective attention heads KV cache on the GPU while computing attention output dynamically. Through roofline analysis, we demonstrate that HEADINFER maintains computational efficiency while significantly reducing memory footprint. We evaluate HEADINFER on the Llama-3-8B model with a 1-million-token sequence, reducing the GPU memory footprint of the KV cache from 128 GB to 1 GB and the total GPU memory usage from 207 GB to 17 GB, achieving a 92% reduction compared to BF16 baseline inference. Notably, HEADINFER enables 4-million-token inference with an 8B model on a single consumer GPU with 24GB memory (e.g., NVIDIA RTX 4090) without approximation methods.

**Keywords:** Transformer-based large language models, long context generation, key-value cache, KV cache, HEADINFER, fine-grained, head-wise offloading strategy, roofline analysis, computational efficiency, Llama-3-8B model, NVIDIA RTX 4090

## Reproduce / read

```sh
arxiv read 2502.12574            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2502.12574 "method"
arxiv bibtex 2502.12574
```
