# Composable CXL Memory as a Kubernetes-Native Shared Memory for LLM Serving

**arXiv:** [2609.10790](https://arxiv.org/abs/2609.10790) · [PDF](https://arxiv.org/pdf/2609.10790) · [HTML](https://arxiv.org/html/2609.10790) · **First submitted:** 2026-09-09  
**Category:** [Memory management & offloading (weights, activations)](README.md) — **rank #36 of 39** by impact score  
**Also relevant to:** [Prefill/decode disaggregation & phase-aware serving](../disaggregated-prefill-decode/README.md) · [Request scheduling, batching & SLO serving](../scheduling-and-batching/README.md) · [KV cache — general, systems & analysis](../../kv-cache/_general/README.md)  

> **TL;DR** — A DRA driver is presented that makes composable CXL memory a schedulable cluster resource, and the resulting shared-memory tier for cross-node KV-cache reuse in LLM serving is evaluated, demonstrating memory disaggregation rather than prefill/decode disaggregation.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Hongjian Fan, Kevin Zhang, David Habinsky, Sean Dykstra |
| arXiv categories | cs.DC, cs.LG |
| arXiv comments | 23 pages, 9 figures, 9 tables |
| Citations (Semantic Scholar, 2026-09-25) | 0 (0 influential) |
| Hugging Face upvotes | 0 |
| Code | — |
| Models used | Qwen2.5-7B |
| Impact score | 0.0 |

## Key results (claimed in abstract)

- The sharing gap, defined as the latency ratio between cross-node and same-node reuse, is 1--4\%, indicating that cross-node reuse incurs little additional latency relative to same-node reuse on our testbed.

## Abstract

We present a Kubernetes Dynamic Resource Allocation (DRA) driver that makes composable CXL memory a schedulable cluster resource, and evaluate the resulting shared-memory tier for cross-node KV-cache reuse in LLM serving. The driver composes CXL regions on demand, materializes them as DAX devices on each participating host, and injects them into pods under a single Container Device Interface (CDI) name so that pods on different nodes access the same physical region. A shared-memory connector for vLLM/llm-d uses that region as a KV-cache tier with a slot directory embedded inside the shared medium, which eliminates the need for an external metadata service. On a two-node cluster with a 512\,GiB CXL appliance and Qwen2.5-7B-Instruct, cross-node prefix reuse reduces TTFT by 5.5$\times$--36.6$\times$ at an external hit rate of 95.4--99.5\,\%, while node-local tiers (GPU prefix caching, CPU-DRAM offload) fall back to full recompute. The sharing gap, defined as the latency ratio between cross-node and same-node reuse, is 1--4\%, indicating that cross-node reuse incurs little additional latency relative to same-node reuse on our testbed. Both replicas run full engines; the study demonstrates memory disaggregation rather than prefill/decode disaggregation. We report this as a feasibility study rather than a performance evaluation.

## Reproduce / read

```sh
arxiv read 2609.10790            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2609.10790 "method"
arxiv bibtex 2609.10790
```
