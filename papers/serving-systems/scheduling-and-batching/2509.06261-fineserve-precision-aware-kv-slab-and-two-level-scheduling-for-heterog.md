# FineServe: Precision-Aware KV Slab and Two-Level Scheduling for Heterogeneous Precision LLM Serving

**arXiv:** [2509.06261](https://arxiv.org/abs/2509.06261) · [PDF](https://arxiv.org/pdf/2509.06261) · [HTML](https://arxiv.org/html/2509.06261) · **First submitted:** 2025-09-15  
**Category:** [Request scheduling, batching & SLO serving](README.md) — **rank #115 of 206** by impact score  
**Also relevant to:** [KV cache — general, systems & analysis](../../kv-cache/_general/README.md)  

> **TL;DR** — FineServe, an inference serving framework for mixed-precision LLMs, is proposed, an inference serving framework for mixed-precision LLMs that achieves up to 2.2x higher SLO attainment and 1.8x higher token generation throughput compared to the state-of-the-art GPU sharing systems.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Kyungmin Bin, Seungbeom Choi, Jimyoung Son, Jieun Choi, Daseul Bae, Daehyeon Baek, Kihyo Moon, Minsung Jang, Hyojung Lee |
| arXiv categories | cs.DC, cs.LG |
| Citations (Semantic Scholar, 2026-09-25) | 3 (0 influential) |
| Hugging Face upvotes | 0 |
| Code | — |
| Training requirement (from abstract) | training-free / post-training |
| Models used | Llama-3.1-8B, Llama3.1-8B, Llama-3.1-70B, Llama-3-8B, Qwen-3-32B, Llama3-8B, Llama3.1-70B |
| Hardware | H100 (×1 mentions) |
| Impact score | 0.83 |

## Key results (claimed in abstract)

- FineServe's key contributions include: (1) KV Slab, a precision-aware adaptive memory management technique dynamically allocating KV cache based on model quantization characteristics, significantly reducing GPU memory fragmentation, and (2) a two-level scheduling framework comprising a global scheduler that places models to GPUs based on request rates, latency SLOs, and memory constraints and efficiency, and a local scheduler that adaptively adjusts batch sizes according to real-time request fluctuations.
- Experimental results demonstrate that FineServe achieves up to 2.2x higher SLO attainment and 1.8x higher token generation throughput compared to the state-of-the-art GPU sharing systems.

## Abstract

Recent advances in Post-Training Quantization (PTQ) techniques have significantly increased demand for serving quantized large language models (LLMs), enabling higher throughput and substantially reduced memory usage with minimal accuracy loss. Quantized models address memory constraints in LLMs and enhance GPU resource utilization through efficient GPU sharing. However, quantized models have smaller KV block sizes than non-quantized models, causing limited memory efficiency due to memory fragmentation. Also, distinct resource usage patterns between quantized and non-quantized models require efficient scheduling to maximize throughput. To address these challenges, we propose FineServe, an inference serving framework for mixed-precision LLMs. FineServe's key contributions include: (1) KV Slab, a precision-aware adaptive memory management technique dynamically allocating KV cache based on model quantization characteristics, significantly reducing GPU memory fragmentation, and (2) a two-level scheduling framework comprising a global scheduler that places models to GPUs based on request rates, latency SLOs, and memory constraints and efficiency, and a local scheduler that adaptively adjusts batch sizes according to real-time request fluctuations. Experimental results demonstrate that FineServe achieves up to 2.2x higher SLO attainment and 1.8x higher token generation throughput compared to the state-of-the-art GPU sharing systems.

## Contributions (from the paper)

Recent advances in Post-Training Quantization (PTQ) techniques have significantly increased demand for serving quantized large language models (LLMs), enabling higher throughput and substantially reduced memory usage with minimal accuracy loss. Quantized models address memory constraints in LLMs and enhance GPU resource utilization through efficient GPU sharing. However, quantized models have smaller KV block sizes than non-quantized models, causing limited memory efficiency due to memory fragmentation. Also, distinct resource usage patterns between quantized and non-quantized models require efficient scheduling to maximize throughput. To address these challenges, we propose FineServe, an inference serving framework for mixed-precision LLMs. FineServe's key contributions include: (1) KV Slab, a precision-aware adaptive memory management technique dynamically allocating KV cache based on model quantization characteristics, significantly reducing GPU memory fragmentation, and (2) a two-level scheduling framework comprising a global scheduler that places models to GPUs based on request rates, latency SLOs, and memory constraints and efficiency, and a local scheduler that adaptively adjusts batch sizes according to real-time request fluctuations. Experimental results demonstrate that FineServe achieves up to 2.2x higher SLO attainment and 1.8x higher token generation throughput compared to the state-of-the-art GPU sharing systems.

## Compute & hardware (extracted from full text)

Hardware setup mentions:

- We implement FineServe on top of the vLLM engine and evaluate it on a cluster of up to 16 NVIDIA H100 using the ShareGPT (ShareGPT Team, 2025) dataset.

## Main results tables (from the paper)

**Table 4. Placement Results (Placement Order) of Figure 12**

| Method | GPU 0 | GPU 1 |
| --- | --- | --- |
| FineServe | Model C (1st) | Model A (2nd), Model B (3rd) |
| Prism⋆ | Model B (1st), Model C (3rd) | Model A (2nd) |

**Table 1. Evaluated System Specification.**

| Component | Description |
| --- | --- |
| CPU | 32$\times$ vCPU Intel Xeon Platinum 8468 @ 2.1 GHz, Hyperthreading enabled |
| GPU | 4$\times$ NVIDIA H100 80GB SXM5 (700W) |
| GPU P2P | 900 GB/s NVSwitch |
| PCIe | PCIe Gen5 $\times$16 |
| Memory | DDR5 720 GB allocated |

## Conclusion (from the paper)

We presented FineServe, a mixed-precision LLMs serving framework that addresses memory fragmentation and inefficient GPU sharing. FineServe introduces KV slab, a precision-aware memory manager, and a two-level scheduler that jointly optimize GPU memory and compute allocation. Evaluations with real-world traces show that FineServe achieves up to 2.2\times higher SLO attainment and 1.8\times higher token throughput compared to state-of-the-art GPU sharing systems.

<details><summary>Paper outline</summary>

- Abstract.
- 1. Introduction
- 2. Background
- 2.1. LLM Inference
- 2.2. Post-Training Quantization (PTQ)
- 2.3. PagedAttention
- 2.4. GPU Sharing
- 3. Motivation
- 3.1. Characteristics of Quantized Model Serving
- 3.2. KV Cache Management for Quantized Models
- 3.3. Impacts of GPU Sharing: Throughput and Memory Efficiency
- 4. Overall Architecture of FineServe
- 5. KV Slab: Dynamic KV Cache Management
- 5.1. Rationale of KV Slab
- 5.2. KV Slab Manager
- 6. Two-level Scheduler
- 6.1. Scheduling Principles and Objectives
- 6.2. Global Scheduler: Model-to-GPU
- 6.3. Local Scheduler: Adaptive Batching
- 7. Evaluation
- 7.1. Experimental Setup
- 7.2. End-to-End Performance
- 7.3. Ablation Study: Two-level Scheduler
- 8. Related Work
- 9. Conclusion

</details>

## Reproduce / read

```sh
arxiv read 2509.06261            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2509.06261 "method"
arxiv bibtex 2509.06261
```
