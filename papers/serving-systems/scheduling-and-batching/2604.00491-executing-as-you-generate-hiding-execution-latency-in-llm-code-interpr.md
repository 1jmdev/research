# Executing as You Generate: Hiding Execution Latency in LLM Code Interpreters

**arXiv:** [2604.00491](https://arxiv.org/abs/2604.00491) · [PDF](https://arxiv.org/pdf/2604.00491) · [HTML](https://arxiv.org/html/2604.00491) · **First submitted:** 2026-06-22  
**Category:** [Request scheduling, batching & SLO serving](README.md) — **rank #42 of 206** by impact score  

> **TL;DR** — EAGER, a concrete implementation featuring AST-based chunking, dynamic batching with gated execution, and early error interruption, is presented, a concrete implementation featuring AST-based chunking, dynamic batching with gated execution, and early error interruption.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Zhensu Sun, Zhihao Lin, Zhi Chen, Chengran Yang, Mingyi Zhou, Li Li, David Lo |
| Organisation (HF) | Singapore Management University |
| arXiv categories | cs.PL, cs.AI, cs.SE |
| arXiv comments | 10 pages |
| Citations (Semantic Scholar, 2026-09-25) | 2 (0 influential) |
| Hugging Face upvotes | 3 |
| Code | — |
| Impact score | 2.88 |

## Key results (claimed in abstract)

- The overlap mechanism hides almost all execution behind generation, reducing the non-overlapped portion of execution time by up to 99.8% and cutting end-to-end latency by up to 37.3% on error-free runs.

## Abstract

Current LLM systems are increasingly equipped with a code interpreter that executes generated code to obtain results. This works serially: the model first generates the complete code, then an interpreter executes it. This sequential workflow leaves the executor idle during generation and the generator idle during execution, resulting in unnecessary end-to-end latency. Our key observation is that an LLM, unlike a human developer, emits code tokens left to right and does not backtrack over what it has already written. This makes it possible to start executing a piece of code while later tokens are still being generated. We formalize this parallel execution paradigm, modeling it as a three-stage pipeline of generation, detection, and execution, and derive closed-form latency bounds that characterize its speedup potential and operating regimes. We then present EAGER, a concrete implementation featuring AST-based chunking, dynamic batching with gated execution, and early error interruption. We evaluate EAGER across four benchmarks, seven LLMs, and three execution environments. The overlap mechanism hides almost all execution behind generation, reducing the non-overlapped portion of execution time by up to 99.8% and cutting end-to-end latency by up to 37.3% on error-free runs.

**Keywords:** LLM-based coding agents, serial execution paradigm, code generation, interpreter execution, end-to-end latency, parallel execution, three-stage pipeline, generation detection execution, closed-form latency bounds, AST-based chunking, dynamic batching, gated execution

## Reproduce / read

```sh
arxiv read 2604.00491            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2604.00491 "method"
arxiv bibtex 2604.00491
```
