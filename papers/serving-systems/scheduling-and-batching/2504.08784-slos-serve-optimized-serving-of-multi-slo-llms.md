# SLOs-Serve: Optimized Serving of Multi-SLO LLMs

**arXiv:** [2504.08784](https://arxiv.org/abs/2504.08784) · [PDF](https://arxiv.org/pdf/2504.08784) · [HTML](https://arxiv.org/html/2504.08784) · **First submitted:** 2025-04-05  
**Category:** [Request scheduling, batching & SLO serving](README.md) — **rank #15 of 206** by impact score  

> **TL;DR** — This paper introduces SLOs-Serve, a system designed for serving multi-stage large language model (LLM) requests with application- and stage-specific service level objectives (SLOs) to customize the allocation of tokens to meet these SLO requirements.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Siyuan Chen, Zhipeng Jia, Samira Khan, Arvind Krishnamurthy, Phillip B. Gibbons |
| arXiv categories | cs.DC, cs.LG |
| Citations (Semantic Scholar, 2026-09-25) | 30 (2 influential) |
| Hugging Face upvotes | 0 |
| Code | — |
| Models used | OPT-7B, OPT-13B, Deepseek-R1, OPT-30B |
| Hardware | A100 (×4 mentions), H100 (×3 mentions) |
| Impact score | 4.4 |

## Key results (claimed in abstract)

- Our evaluation across 6 LLM application scenarios (including summarization, coding, chatbot, tool calling, and reasoning) demonstrates that SLOs-Serve improves per-GPU serving capacity by 2.2x on average compared to prior state-of-the-art systems.

## Abstract

This paper introduces SLOs-Serve, a system designed for serving multi-stage large language model (LLM) requests with application- and stage-specific service level objectives (SLOs). The key idea behind SLOs-Serve is to customize the allocation of tokens to meet these SLO requirements. SLOs-Serve uses a multi-SLO dynamic programming-based algorithm to continuously optimize token allocations under SLO constraints by exploring the full design space of chunked prefill and (optional) speculative decoding. Leveraging this resource planning algorithm, SLOs-Serve effectively supports multi-SLOs and multi-replica serving with dynamic request routing while being resilient to bursty arrivals. Our evaluation across 6 LLM application scenarios (including summarization, coding, chatbot, tool calling, and reasoning) demonstrates that SLOs-Serve improves per-GPU serving capacity by 2.2x on average compared to prior state-of-the-art systems.

## Compute & hardware (extracted from full text)

Hardware setup mentions:

- For the end-to-end capacity evaluation, we use the Google a2-highgpu-4g VM with 4 NVIDIA 40GB A100 GPUs, connected with pairwise NVLINK.
- For the scalability evaluation, we use an additional Google a3-highgpu-8g with 8 NVIDIA 80GB H100 GPU to perform multi-replica serving for the OPT-13B model.
- Now, we evaluate SLOs-Serve’s multi-replica on the cluster with 4 A100 GPUs, serving the OPT-7B model with up to 4 replicas.

## Main results tables (from the paper)

**Table 1. Multi-stage LLM applications with diverse SLOs.**

| Application | Stages | SLOs |
| --- | --- | --- |
| Summarization | Long, fast prompt processing Short reading-speed response | Tight on prefill Loose on decode |
| Coder | Normal length/speed prompt Long, fast response | Loose on prefill Tight on decode |
| ChatBot | Normal length/speed prompt Reading-speed response | Loose on prefill Loose on decode |
| LLM with Tools | Normal length/speed prompt Fast toolCall-toolResponse loop Reading-speed response | Tight on prefill Tight on pref./dec. Loose on decode |
| Reasoning LLM | Normal length/speed prompt Long fast thinking Reading-speed response | Tight on prefill Tight on decode Loose on decode |

**Table 2. Experiment Scenarios.**

| Scenarios | Arrival Pattern | Prompt Dataset | Model |
| --- | --- | --- | --- |
| ChatBot | Azure-Chatting (Patel et al., 2024) | ShareGPT (sha, [n. d.]) | Main: OPT-7B/13B/30B (Zhang et al., 2022) |
| Coder | Azure-Coding (Patel et al., 2024) | HumanEval (Chen et al., 2021) | Main: OPT-7B/13B/30B (Zhang et al., 2022) |
| Summarizer | Azure-Chatting | Arxiv Summary (Cohan et al., 2018) | Spec: OPT-125m |
| Mixed | ChatBot + Coder + Summarizer | ChatBot + Coder + Summarizer | ChatBot + Coder + Summarizer |
| ToolLLM | Azure-Coding | ToolBench (Guo et al., 2024) | ToolLlama-7B (Qin et al., 2023) |
| Reasoning | Azure-Chatting | s1K (Muennighoff et al., 2025) | Deepseek-R1-Qwen-1.5B (Guo et al., 2025) |

## Conclusion (from the paper)

In this work, we built SLOs-Serve, a distributed LLM serving system that supports multi-SLOs and multi-replica serving, while being resilient to burst arrivals. Particularly, we solve the key challenge of serving under application- and stage-dependent SLOs in a resource-sharing environment by customizing token allocations in batches. SLOs-Serve then designs a dynamic programming-based algorithm that effectively detects requests with unattainable SLOs and continuously optimizes the token compositions by exploring the full design space of chunked prefill and (optional) speculative decoding. Built on top of the scheduler, SLOs-Serve enabled burst handling and low overhead routing in multi-replica serving. In evaluation, comprehensive studies across six applications showed on average 2.2x improvement in serving capacity. In multi-replica serving, SLOs-Serve embodied super-linear scaling with bursty arrivals, underscoring the effectiveness of dynamic request routing.

<details><summary>Paper outline</summary>

- Abstract.
- 1. Introduction
- 2. Background and Motivation
- 2.1. LLM Applications and Serving
- 2.2. LLM Serving Optimizations
- 2.3. Challenges
- 3. SLOs-Serve Scheduler
- 3.1. Scheduling with Soft Admission Control
- 3.2. SLOs-Serve’s scheduling algorithm
- 4. SLOs-Serve
- 4.1. Burst Resilient Scheduling
- 4.2. Multi-replica Serving with SLO-driven Request Routing
- 5. Implementation
- 6. Evaluation
- 6.1. End-to-end Capacity Evaluation
- 6.2. Scaling Evaluation
- 6.3. Ablation Study
- 6.4. Scheduling Overhead
- 6.5. Fidelity of the performance model
- 7. Related Work
- 8. Conclusion
- 9. Acknowledgement
- Appendix A Detailed Analysis into the disaggregated Scheduling.
- Appendix B More on LLM Serving
- Appendix C Time Complexity Analysis
- Appendix D SLO-adaptive Speculative Decoding

</details>

## Reproduce / read

```sh
arxiv read 2504.08784            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2504.08784 "method"
arxiv bibtex 2504.08784
```
