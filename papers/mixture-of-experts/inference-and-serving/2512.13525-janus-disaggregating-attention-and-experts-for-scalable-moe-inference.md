# Janus: Disaggregating Attention and Experts for Scalable MoE Inference

**arXiv:** [2512.13525](https://arxiv.org/abs/2512.13525) · [PDF](https://arxiv.org/pdf/2512.13525) · [HTML](https://arxiv.org/html/2512.13525) · **First submitted:** 2026-08-11  
**Category:** [MoE inference & serving (expert offloading, EP)](README.md) — **rank #4 of 168** by impact score  
**Also relevant to:** [MoE architecture & routing](../architecture-and-routing/README.md)  

> **TL;DR** — JANUS is presented, a scalable and resource-efficient MoE inference system built around three key principles that employs a fine-grained, SLO-aware resource scaling scheme that jointly selects attention resources, MoE resources, and expert placement to minimize GPU cost under token-level SLOs.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Zhexiang Zhang, Ye Wang, Yumiao Zhao, Jiayu Xiao, Qianjing Yang, Xiangyu Wang, Jingzhe Jiang, Qizhen Weng, Ruichuan Chen, Shaohuai Shi, … (+2) |
| Organisation (HF) | Chinese University of Hong Kong, Shenzhen |
| arXiv categories | cs.DC |
| arXiv comments | v4: Author list updated to correct an error in v3 |
| Citations (Semantic Scholar, 2026-09-25) | 9 (0 influential) |
| Hugging Face upvotes | 2 |
| Code | — |
| Impact score | 5.7 |

## Key results (claimed in abstract)

- Evaluation shows that JANUS improves per-GPU throughput by up to 4.7x over state-of-the-art MoE inference baselines while satisfying token-level latency SLOs.

## Abstract

Serving large Mixture-of-Experts (MoE) models is challenging because of their large memory footprints, heterogeneous resource demands, and highly dynamic inference workloads. Most existing MoE inference systems deploy the entire model as a monolithic unit, forcing attention and MoE layers to share the same resource configuration despite their different scaling behaviors and resource bottlenecks. Such coarse-grained provisioning leads to resource inefficiency and suboptimal performance. We present JANUS, a scalable and resource-efficient MoE inference system built around three key principles. First, JANUS disaggregates attention and MoE layers onto separate GPU worker pools, enabling independent resource provisioning for the two layer types, and uses an adaptive two-phase communication mechanism for low-latency data exchange. Second, because MoE-layer execution is often memory-bound and highly sensitive to activated-expert imbalance, JANUS introduces a lightweight, microsecond-scale activation scheduler that balances per-layer activated experts across MoE instances to reduce inference latency. Third, JANUS employs a fine-grained, SLO-aware resource scaling scheme that jointly selects attention resources, MoE resources, and expert placement to minimize GPU cost under token-level SLOs. Evaluation shows that JANUS improves per-GPU throughput by up to 4.7x over state-of-the-art MoE inference baselines while satisfying token-level latency SLOs.

**Keywords:** Mixture-of-Experts, MoE, GPU sub-clusters, adaptive two-phase communication, lightweight scheduler, GPU kernel, fine-grained resource management, expert placement, per-GPU throughput, per-token latency

## Reproduce / read

```sh
arxiv read 2512.13525            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2512.13525 "method"
arxiv bibtex 2512.13525
```
