# LayerRoute: Input-Conditioned Adaptive Layer Skipping via LoRA Fine-Tuning for Agentic Language Models

**arXiv:** [2606.01838](https://arxiv.org/abs/2606.01838) · [PDF](https://arxiv.org/pdf/2606.01838) · [HTML](https://arxiv.org/html/2606.01838) · **First submitted:** 2026-09-06  
**Category:** [Early exit, layer skipping & dynamic depth](README.md) — **rank #18 of 43** by impact score  
**Also relevant to:** [Parameter-efficient fine-tuning (LoRA & friends)](../../training/parameter-efficient-finetuning/README.md)  

> **TL;DR** — LayerRoute is a lightweight adapter that selectively skips transformer blocks during inference based on input type, achieving compute savings while maintaining or improving model quality through gated routing and LoRA adaptation.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Prateek Kumar Sikdar |
| Organisation (HF) | Accenture |
| arXiv categories | cs.CL, cs.AI, cs.LG |
| arXiv comments | Central claim -- 12.91% skip-rate differential between tool-call/planning inputs (Sec. 5.1, Table 2) -- does not reproduce across 10 independently-seeded runs (range -19.6% to +5.1%, mean -5.7%); the single reported run was not representative. Router shows negligible alignment with the claimed input |
| Citations (Semantic Scholar, 2026-09-25) | 0 (0 influential) |
| Hugging Face upvotes | 1 |
| Code | https://github.com/hackie123/layerroute |
| Training requirement (from abstract) | involves (re)training / fine-tuning |
| Models used | Qwen2.5-0.5B |
| Hardware | A100 |
| Impact score | 1.26 |

## Key results (claimed in abstract)

- After 3,000 steps (6.4 minutes on an A100 40GB), LayerRoute achieves a 12.91% skip differential: tool calls skip 15.25% of FLOPs while planning steps skip only 2.34%, using only 1.10M trainable parameters (0.22% of the 494M backbone).
- Quality improves over the base model due to LoRA adaptation, with perplexity delta of -1.29 on tool calls and -1.30 on planning.

## Abstract

Agentic language model systems alternate between two structurally distinct step types: structured tool calls (short, deterministic, low perplexity) and open-ended planning/reasoning steps (long, complex, high perplexity). Despite this heterogeneity, current inference systems apply identical compute to every step. We introduce LayerRoute, a lightweight adapter that learns to selectively skip transformer blocks on a per-input basis. LayerRoute augments each of the 24 transformer blocks in Qwen2.5-0.5B-Instruct with: (1) a per-layer router (~897 parameters, Linear(896,1)) that outputs a hard binary gate via the straight-through estimator, and (2) LoRA adapters (rank 8, ~1.08M parameters) on the Q/K/V/O attention projections. The backbone weights remain frozen. A single end-to-end training pass on agentic data (Hermes, Glaive, GSM8K, Turing) with a gate regularisation term forces the system to discover which blocks are skippable per input type. After 3,000 steps (6.4 minutes on an A100 40GB), LayerRoute achieves a 12.91% skip differential: tool calls skip 15.25% of FLOPs while planning steps skip only 2.34%, using only 1.10M trainable parameters (0.22% of the 494M backbone). Quality improves over the base model due to LoRA adaptation, with perplexity delta of -1.29 on tool calls and -1.30 on planning.

**Keywords:** transformer blocks, structured tool calls, open-ended planning, perplexity, LayerRoute, router, straight-through estimator, LoRA adapters, attention projections, gated routing, FLOPs, backbone weights

## Reproduce / read

```sh
arxiv read 2606.01838            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2606.01838 "method"
arxiv bibtex 2606.01838
```
