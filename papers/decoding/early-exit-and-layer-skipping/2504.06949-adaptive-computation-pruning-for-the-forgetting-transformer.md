# Adaptive Computation Pruning for the Forgetting Transformer

**arXiv:** [2504.06949](https://arxiv.org/abs/2504.06949) · [PDF](https://arxiv.org/pdf/2504.06949) · [HTML](https://arxiv.org/html/2504.06949) · **First submitted:** 2025-08-11  
**Category:** [Early exit, layer skipping & dynamic depth](README.md) — **rank #10 of 45** by impact score  
**Also relevant to:** [Structured pruning (layers, heads, width, experts)](../../compression/structured-pruning/README.md)  

> **TL;DR** — This work proposes Adaptive Computation Pruning for FoX, a method that dynamically prunes computations involving input-output dependencies that are strongly decayed by the forget gate and applies it to language model pretraining with FoX and shows it consistently reduces the number of FLOPs and memory accesses in softmax attention.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Zhixuan Lin, Johan Obando-Ceron, Xu Owen He, Aaron Courville |
| arXiv categories | cs.LG, cs.AI, cs.CL |
| Venue | Published as a conference paper at COLM 2025 |
| arXiv comments | Published as a conference paper at COLM 2025 |
| Citations (Semantic Scholar, 2026-09-25) | 5 (0 influential) |
| Hugging Face upvotes | 3 |
| Code | https://github.com/zhixuan-lin/forgetting-transformer |
| Training requirement (from abstract) | involves (re)training / fine-tuning |
| Impact score | 3.94 |

## Key results (claimed in abstract)

- We apply ACP to language model pretraining with FoX and show it consistently reduces the number of FLOPs and memory accesses in softmax attention by around 70% across different model sizes and context lengths, resulting in a roughly 50% to 70% reduction in attention runtime (or a 2-3$\times$ speedup) and a roughly 10% to 40% increase in end-to-end training throughput.

## Abstract

The recently proposed Forgetting Transformer (FoX) incorporates a forget gate into softmax attention and has shown consistently better or on-par performance compared to the standard RoPE-based Transformer. Notably, many attention heads in FoX tend to forget quickly, causing their output at each timestep to rely primarily on local context. Based on this observation, we propose Adaptive Computation Pruning (ACP) for FoX, a method that dynamically prunes computations involving input-output dependencies that are strongly decayed by the forget gate. In particular, our method performs provably safe pruning via a dynamically set pruning threshold that guarantees the pruned attention weights are negligible. We apply ACP to language model pretraining with FoX and show it consistently reduces the number of FLOPs and memory accesses in softmax attention by around 70% across different model sizes and context lengths, resulting in a roughly 50% to 70% reduction in attention runtime (or a 2-3$\times$ speedup) and a roughly 10% to 40% increase in end-to-end training throughput. Furthermore, longer context lengths yield greater computational savings. All these speed improvements are achieved without any performance degradation. Our code is available at https://github.com/zhixuan-lin/forgetting-transformer.

**Keywords:** Forgetting Transformer, FoX, forget gate, softmax attention, Adaptive Computation Pruning, ACP, input-output dependencies, pruning threshold, FLOPs, training throughput, context lengths, pruning patterns

## Reproduce / read

```sh
arxiv read 2504.06949            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2504.06949 "method"
arxiv bibtex 2504.06949
```
