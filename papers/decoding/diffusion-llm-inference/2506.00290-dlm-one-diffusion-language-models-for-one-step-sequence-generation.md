# DLM-One: Diffusion Language Models for One-Step Sequence Generation

**arXiv:** [2506.00290](https://arxiv.org/abs/2506.00290) · [PDF](https://arxiv.org/pdf/2506.00290) · [HTML](https://arxiv.org/html/2506.00290) · **First submitted:** 2026-09-02  
**Category:** [Diffusion LLM inference acceleration](README.md) — **rank #31 of 203** by impact score  
**Also relevant to:** [Knowledge distillation (LLM → smaller LLM)](../../compression/knowledge-distillation/README.md)  

> **TL;DR** — DLM-One eliminates iterative refinement by aligning the scores of a student model's outputs with the score function of a pretrained teacher DLM in the forward-diffused noisy space, which positions one-step score distillation as a viable path for the efficient deployment of continuous diffusion models operating in continuous space for natural language processing.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Tianqi Chen, Shujian Zhang, Mingyuan Zhou |
| arXiv categories | cs.CL, cs.LG, stat.ML |
| Citations (Semantic Scholar, 2026-09-25) | 16 (0 influential) |
| Hugging Face upvotes | 0 |
| Code | https://github.com/Shark-NLP/DiffuSeq |
| Training requirement (from abstract) | involves (re)training / fine-tuning |
| Models used | GPT-2, LLaDA |
| Hardware | B200 (×1 mentions) |
| Impact score | 6.73 |

## Key results (claimed in abstract)

- Through experiments on multiple representative DLMs, we show that DLM-One achieves up to $\sim$2000$\times$ speedup in sampling steps and $\sim$500$\times$ in wall-clock time, while maintaining competitive performance on benchmark text generation tasks.

## Abstract

This paper introduces DLM-One, a score-distillation-based framework for one-step sequence generation with continuous diffusion language models (DLMs). DLM-One eliminates iterative refinement by aligning the scores of a student model's outputs with the score function of a pretrained teacher DLM in the forward-diffused noisy space. We demonstrate that our framework is architecture-agnostic and robust across diverse continuous manifolds, including standard token embedding spaces and logit simplex spaces. Through experiments on multiple representative DLMs, we show that DLM-One achieves up to $\sim$2000$\times$ speedup in sampling steps and $\sim$500$\times$ in wall-clock time, while maintaining competitive performance on benchmark text generation tasks. We further analyze failure modes in language-domain diffusion distillation and propose an adversarially-regularized two-stage training scheme to prevent student degeneration. Our findings position one-step score distillation as a viable path for the efficient deployment of continuous diffusion models operating in continuous space for natural language processing.

## Contributions (from the paper)

Our contributions are summarized as follows: We introduce DLM-One, a practical score distillation framework for continuous DLMs that enables one-step sequence generation without iterative denoising. We propose a two-stage training strategy with adversarial stabilization to enhance student quality and address common failure modes in distilling DLMs in a data-free setting. Our empirical evaluation across architectures and continuous spaces demonstrates that DLM-One achieves competitive performance while reducing sampling steps by up to \mathord{\sim}2000\times and wall-clock time by up to \mathord{\sim}500\times compared to standard iterative DLM teachers.

## Main results tables (from the paper)

**Table 6: Distillation-related hyperparameters used in Stage 1 and Stage 2 across different datasets.**

| Dataset | Stage | $\mu$ | $[t_{\text{min}},t_{\text{max}}]$ | $t_{\text{init}}$ | $a^{sg}_{dsm}$, $b^{sg}_{adv}$ | $a^{g}_{sd}$, $b^{g}_{adv}$ | $\text{lr}_{\psi}$ | $\text{lr}_{\theta}$ |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| QQP | Stage 1 | 1.2 | $[0,1976]$ | 1490 | 0.5, 0.5 | 0.5, 0.5 | 3e-5 | 1e-5 |
| QQP | Stage 2 | 0.5 | $[0,1976]$ | 1490 | 0.5, 0.5 | 0.9, 0.1 | 1e-5 | 1e-5 |
| Q-T | Stage 1 | 1.2 | $[0,1976]$ | 1490 | 0.5, 0.5 | 0.5, 0.5 | 1e-5 | 1e-5 |
| Q-T | Stage 2 | 1.2 | $[0,1976]$ | 1490 | 0.5, 0.5 | 0.5, 0.5 | 1e-5 | 1e-5 |
| Wiki | Stage 1 | 1.0 | $[0,1976]$ | 1490 | 0.5, 0.5 | 0.5, 0.5 | 1e-5 | 1e-5 |

**Table 11: Examples from the Paraphrase (PP) task. Each example consists of a source sentence, a reference sentence, and outputs generated by DiffuSeq (Teacher) and DLM-One (Student).**

| Source | Reference | Recover | Recover |
| --- | --- | --- | --- |
|  |  | DiffuSeq | DLM-One |
| how can i be a good geologist? | what should i do to be a great geologist? | how do i really be a good geologist? | how can i become a good geologist? |
| which are the best engineering fields? | what is the best field of engineering? | which are the best engineering field? | what are the best engineering fields? |
| how do i become an attractive girl? | how do you become pretty / attractive? | how can one become a girl? | how can i become an attractive girl quickly? |
| how does a long distance relationship work? | do long distance relationships work? | does long distance relationship work? | how do i have a long distance relationship? |
| what are some interesting things to do when bored? | what should i do if i’m badly bored? | what should you do when you bored? | what are the best thing to do when bored? |

## Conclusion (from the paper)

In this work, we propose a practical distillation framework for training continuous diffusion language models for one-step sequence generation (DLM-One), eliminating the need for iterative refinement during generation. Our method is broadly applicable to continuous DLMs and enables fast, one-step generation via score distillation from pretrained teacher models. To further stabilize training and improve student quality, we introduce a two-stage training scheme with adversarial regularization. Through detailed experiments on conditional text generation tasks, we demonstrate that DLM-One achieves competitive performance against the teacher DLMs while reducing sampling cost by up to \mathord{\sim}500\times. This redefines the Pareto frontier between computational efficiency and generation quality in continuous diffusion-based language modeling, and has profound implications for the future development of LLMs. Nevertheless, our work opens up several promising directions for future investigation. First, while hyperparameters like the score distillation loss coefficient \mu currently require per-task tuning, future work could explore more principled and adaptive training schemes. Second, we find that the trade-off between fidelity and diversity in DLM-One is a controllable aspect that can be adjusted based on the needs of downstream applications. We identify the extension of DLM-One …

<details><summary>Paper outline</summary>

- 1 Introduction
- 2 Related Work
- 2.1 Diffusion Language Models
- 2.2 Faster Diffusion
- 3 One-Step Diffusion Language Models
- 3.1 Embedding-space Score Distillation
- 3.2 Adversarial Regularization
- 3.3 Two-stage Training
- 4 Experiments
- 4.1 Tasks and Datasets
- 4.2 Evaluation
- 4.3 Sequence-to-Sequence (Seq2Seq) Tasks
- 4.4 Unconditional Text Generation
- 5 Discussion
- 6 Conclusion
- Appendix A Broader Impacts
- Appendix B Implementation Details
- B.1 DiffuSeq
- Appendix C On the Fidelity-Diversity Trade-off
- C.1 Mitigating Diversity Loss with Text Augmentation
- C.2 Improving Diversity with Multi-Step Training
- Appendix D Comparison with Autoregressive (AR) Models
- Appendix E Pseudo Code
- Appendix F Case Study: TESS Diffusion
- Appendix G Additional Results
- G.1 Generated Samples for Seq2Seq Tasks
- G.2 DLM-One with MBR Decoding
- Appendix H LLM Usage Statement

</details>

## Reproduce / read

```sh
arxiv read 2506.00290            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2506.00290 "method"
arxiv bibtex 2506.00290
```
