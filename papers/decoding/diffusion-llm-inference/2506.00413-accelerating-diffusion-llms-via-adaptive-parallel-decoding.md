# Accelerating Diffusion LLMs via Adaptive Parallel Decoding

**arXiv:** [2506.00413](https://arxiv.org/abs/2506.00413) · [PDF](https://arxiv.org/pdf/2506.00413) · [HTML](https://arxiv.org/html/2506.00413) · **First submitted:** 2025-10-30  
**Category:** [Diffusion LLM inference acceleration](README.md) — **rank #10 of 203** by impact score  
**Also relevant to:** [Diffusion language models (dLLMs) — models & training](../diffusion-language-models/README.md) · [Jacobi, lookahead & consistency-based parallel decoding](../jacobi-and-parallel-decoding/README.md)  

> **TL;DR** — Adapt parallel decoding (APD) is introduced, a novel method that dynamically adjusts the number of tokens sampled in parallel by defining a multiplicative mixture between the dLLM marginal probabilities and the joint probability of sequences under a small auxiliary autoregressive model.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Daniel Israel, Guy Van den Broeck, Aditya Grover |
| arXiv categories | cs.CL, cs.AI, cs.LG, cs.PF |
| Venue | Neural Information Processing Systems (Neural Inf Process Syst) |
| arXiv comments | 10 pages, 5 figures |
| Citations (Semantic Scholar, 2026-09-25) | 109 (7 influential) |
| Hugging Face upvotes | 10 |
| Code | https://github.com/danielmisrael/apd |
| Models used | Dream-7, Llada, Qwen2.5-7B, Qwen2.5-0.5B |
| **Compute (est. H100-hours)** | **57k** (auto-extracted, see below) |
| Impact score | 12.54 |

## Abstract

The generation speed of LLMs are bottlenecked by autoregressive decoding, where tokens are predicted sequentially one by one. Alternatively, diffusion large language models (dLLMs) theoretically allow for parallel token generation, but in practice struggle to achieve the speed of autoregressive models without significantly sacrificing quality. We therefore introduce adaptive parallel decoding (APD), a novel method that dynamically adjusts the number of tokens sampled in parallel. We achieve this by defining a multiplicative mixture between the dLLM marginal probabilities and the joint probability of sequences under a small auxiliary autoregressive model. This inverts the standard setup of speculative decoding, where the goal is to sample from a large autoregressive verifier by drafting from a smaller model. We further optimize APD by enabling KV caching and limiting the size of the masked input. Altogether, our method puts forward three tunable parameters to flexibly tradeoff throughput and quality. We show that APD provides markedly higher throughput with minimal quality degradations on downstream benchmarks.

## Contributions (from the paper)

Our contributions extend beyond the conceptual framework of APD. We further optimize the decoding process by incorporating practical improvements to diffusion model sampling such as Key-Value (KV) caching, a technique traditionally associated with autoregressive models [32]. We also gain significant speed by limiting the size of the masked input to the model. These simple changes substantially increase the computational efficiency of diffusion model inference.

## Compute & hardware (extracted from full text)

Estimates are normalised to H100-hours with the factors in [METHODOLOGY.md](../../../METHODOLOGY.md). Always check the quoted sentence: the parser can't tell which experiment a number belongs to.

| Est. H100-h | Parsed | Source sentence |
| --- | --- | --- |
| 57k | 5000× unspecified × 11.4 h | For the following experiments, we load the models in BF16 precision and run them on single NVIDIA 24GB A5000 GPU connected to a Colfax CX41060s-EK9 4U Rackmount Server with AMD EPYC (Genoa) 9124 processors. |

## Main results tables (from the paper)

**Table 1: dLLM Quality and Throughput with Different Decoding Approaches**

| Model | GMS8K Accuracy | Throughput (tokens/sec) |
| --- | --- | --- |
| Dream 7B (Random, 256 Steps) | $0.404\pm 0.021$ | $3.31\pm 0.068$ |
| Dream 7B (Entropy, 128 Steps) | $0.708\pm 0.020$ | $7.57\pm 0.157$ |
| Dream 7B (Entropy, 256 Steps) | $0.804\pm 0.017$ | $4.28\pm 0.080$ |
| Dream 7B (Left to Right, 256 Steps) | $0.832\pm 0.016$ | $10.1\pm 0.015$ |
| Llada 8B (Random, 256 Steps) | $0.456\pm 0.022$ | $5.07\pm 0.168$ |
| Llada 8B (Confidence, 128 Steps) | $0.526\pm 0.022$ | $13.6\pm 0.284$ |
| Llada 8B (Confidence, 256 Steps) | $0.534\pm 0.022$ | $6.63\pm 0.143$ |
| Llada 8B (Left to Right, 256 Steps) | $0.712\pm 0.020$ | $9.33\pm 0.016$ |
| Qwen2.5 7B (Autoregressive) | $\mathbf{0.854\pm 0.015}$ | $\mathbf{38.6\pm 0.004}$ |

**Table 2: APD Generation Statistics. Mean number of tokens sampled in parallel.**

| Model | GMS8K | GPQA | MATH |
| --- | --- | --- | --- |
| Dream 7B ($R=0.7,W=16,M=100$) | $7.62\pm 0.14$ | $4.35\pm 0.03$ | $6.45\pm 0.04$ |
| Dream 7B ($R=0.6,W=32,M=200$) | $5.75\pm 0.05$ | $4.31\pm 0.02$ | $6.33\pm 0.03$ |

## Conclusion (from the paper)

In this work, we pursue the significant challenge of increasing inference speed in large language models. We introduced Adaptive Parallel Decoding (APD), a novel algorithm that enables substantially faster sampling from dLLMs. APD uniquely restructures the dLLM into a left-to-right autoregressive process and leverages a smaller autoregressive model to assess the quality of parallel-generated token candidates. Complemented by optimizations such as KV caching and limited masked inputs, APD offers tunable parameters that allow a flexible trade-off between generation speed and output quality. This research offers a significant advancement in making dLLMs a more viable and efficient alternative for fast text generation.

## Limitations (from the paper)

Adaptive Parallel Decoding offers a tradeoff, not a free lunch. Higher throughput will lead to lower quality, which is a limitation. We also do not claim to improve over the base diffusion model that we are sampling from. If Dream 7B is weak in a particular domain, our method will also perform poorly.

<details><summary>Paper outline</summary>

- 1 Introduction
- 2 Background
- 2.1 Discrete Diffusion Language Models
- 2.2 From Sequential to Parallel Sampling
- 3 Method
- 3.1 Problem Statement
- 3.2 Adaptive Parallel Decoding
- 3.3 Recompute KV Window
- 3.4 Maximum Masked Lookahead
- 4 Experiments
- 4.1 Implementation
- 4.2 Experimental Configuration
- 4.3 Tradeoffs
- 4.4 Pareto Frontier
- 4.5 Qualitative Examples
- 5 Related Work
- 6 Conclusion
- 7 Acknowledgment
- Appendix A Limitations
- Appendix B Recompute KV Tradeoff
- Appendix C Experimental Details
- Appendix D Qualitative Examples

</details>

**Keywords:** autoregressive decoding, diffusion large language models, dLLMs, adaptive parallel decoding, APD, marginal probabilities, joint probability, speculative decoding, KV caching, masked input

## Reproduce / read

```sh
arxiv read 2506.00413            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2506.00413 "method"
arxiv bibtex 2506.00413
```
