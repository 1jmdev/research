# Accelerating Speculative Decoding with Block Diffusion Draft Trees

**arXiv:** [2604.12989](https://arxiv.org/abs/2604.12989) · [PDF](https://arxiv.org/pdf/2604.12989) · [HTML](https://arxiv.org/html/2604.12989) · **First submitted:** 2026-04-14  
**Category:** [Speculative decoding (draft–verify)](README.md) — **rank #7 of 318** by impact score  
**Also relevant to:** [Diffusion language models (dLLMs) — models & training](../diffusion-language-models/README.md)  

> **TL;DR** — This work introduces DDTree (Diffusion Draft Tree), a method that constructs a draft tree directly from the per-position distributions of a block diffusion drafter, and places it among the leading approaches to speculative decoding.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Liran Ringel, Yaniv Romano |
| arXiv categories | cs.CL |
| Citations (Semantic Scholar, 2026-09-25) | 20 (4 influential) |
| Hugging Face upvotes | 8 |
| Code | https://github.com/liranringel/ddtree |
| Impact score | 9.46 |

## Key results (claimed in abstract)

- DFlash shows that a block diffusion drafter can generate an entire draft block in a single forward pass and achieve state-of-the-art speculative decoding performance, outperforming strong autoregressive drafters such as EAGLE-3.

## Abstract

Speculative decoding accelerates autoregressive language models by using a lightweight drafter to propose multiple future tokens, which the target model then verifies in parallel. DFlash shows that a block diffusion drafter can generate an entire draft block in a single forward pass and achieve state-of-the-art speculative decoding performance, outperforming strong autoregressive drafters such as EAGLE-3. Vanilla DFlash, however, still verifies only a single drafted trajectory per round, potentially limiting its acceptance length. We introduce DDTree (Diffusion Draft Tree), a method that constructs a draft tree directly from the per-position distributions of a block diffusion drafter. Under a fixed node budget, DDTree uses a simple best-first heap algorithm to select the continuations that are most likely to match the target model according to a surrogate defined by the draft model's output. The resulting tree is verified efficiently in a single target model forward pass using an ancestor-only attention mask. Because DDTree builds on DFlash, a leading draft model for speculative decoding, these gains place DDTree among the leading approaches to speculative decoding.

**Keywords:** speculative decoding, autoregressive language models, drafter, draft tree, block diffusion drafter, best-first heap algorithm, ancestor-only attention mask

## Reproduce / read

```sh
arxiv read 2604.12989            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2604.12989 "method"
arxiv bibtex 2604.12989
```
