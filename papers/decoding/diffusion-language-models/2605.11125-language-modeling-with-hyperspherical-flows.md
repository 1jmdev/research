# Language Modeling with Hyperspherical Flows

**arXiv:** [2605.11125](https://arxiv.org/abs/2605.11125) · [PDF](https://arxiv.org/pdf/2605.11125) · [HTML](https://arxiv.org/html/2605.11125) · **First submitted:** 2026-06-01  
**Category:** [Diffusion language models (dLLMs) — models & training](README.md) — **rank #101 of 351** by impact score  

## At a glance

| Field | Value |
| --- | --- |
| Authors | Justin Deschenaux, Caglar Gulcehre |
| arXiv categories | cs.LG |
| Citations (Semantic Scholar, 2026-09-25) | 9 (1 influential) |
| Hugging Face upvotes | 0 |
| Code | https://github.com/jdeschena/s-flm |
| Models used | GPT-2, GPT-3 |
| Hardware | H100 (×3 mentions), L40S/RTX6000Ada (×1 mentions) |
| **Compute (est. H100-hours)** | **0.00 – 0.74** (auto-extracted, see below) |
| Impact score | 4.4 |

## Key results (claimed in abstract)

- Previous FLMs match AR in Generative Perplexity (Gen.\ PPL), but samples with high likelihood are not necessarily correct in verifiable domains such as math and code. $\mathbb{S}$-FLM substantially improves continuous flow language models on large-vocabulary reasoning and closes the gap to masked diffusion under standard-temperature sampling ($T=1$), while a gap remains under optimized low-temperature ($T=0.1$) decoding.

## Abstract

Discrete Diffusion Language Models progressed rapidly as an alternative to autoregressive (AR) models, motivated by their parallel generation abilities. However, for tractability, discrete diffusion models sample from a factorized distribution, which is less expressive than AR. Recent Flow Language Models (FLMs) apply continuous flows to language, transporting noise to data with a deterministic ODE that avoids factorized sampling. FLMs operate on one-hot vectors whose dimension scales with the vocabulary size, making FLMs costly to train. Moreover, since all distinct one-hot embeddings are equidistant in $\ell_2$, adding Gaussian noise does not have a clear semantic interpretation (unlike images, where Gaussian noise progressively degrades structure). We introduce $\mathbb{S}$-FLM, a latent FLM in the hypersphere. $\mathbb{S}$-FLM generates sequences by rotating vectors in $\mathbb{S}^{d-1}$ along a velocity field learned with cross-entropy, avoiding the overhead of materializing one-hot vectors. Previous FLMs match AR in Generative Perplexity (Gen.\ PPL), but samples with high likelihood are not necessarily correct in verifiable domains such as math and code. $\mathbb{S}$-FLM substantially improves continuous flow language models on large-vocabulary reasoning and closes the gap to masked diffusion under standard-temperature sampling ($T=1$), while a gap remains under optimized low-temperature ($T=0.1$) decoding.

## Contributions (from the paper)

We introduced \mathbb{S}-FLM, a Riemannian flow on \mathbb{S}^{d-1} that is competitive at language modeling with large vocabularies and learns the velocity and embeddings jointly. Beyond the formalism, our key contributions include the \mathbb{S}-arch backbone with normalized activations and the truncated and adaptive noise-schedule analysis. On Sudoku, \mathbb{S}-FLM performs similarly to prior FLMs. On GSM8K, where other FLMs fail, \mathbb{S}-FLM with top-1 decoding closes the gap to MDLM and Duo at T=1, though a gap remains at low-temperature (T=0.1) sampling. On OpenWebText, \mathbb{S}-FLM follows the Gen. PPL / Entropy frontier of prior FLMs at high NFE and beats them at low NFE where prior continuous models have an unstable frontier. \mathbb{S}-FLM avoids materializing |\mathcal{V}|-dimensional one-hot vectors, so it trains faster than prior FLMs and may be easier to scale (though this is speculation and left for future work). Our analysis of the sampling-dynamics provides a principled heuristic for truncating the noise schedule that works well in practice. At the same time, there remains a clear gap between diffusion models and AR models in GSM8K.

## Compute & hardware (extracted from full text)

Estimates are normalised to H100-hours with the factors in [METHODOLOGY.md](../../../METHODOLOGY.md). Always check the quoted sentence: the parser can't tell which experiment a number belongs to.

| Est. H100-h | Parsed | Source sentence |
| --- | --- | --- |
| 0.00 | 1× L40S/RTX6000Ada × 0.0111 h | We use a single L40S GPU and train for less than 2 hours. |
| 0.74 | 1× L40S/RTX6000Ada × 2 h | We use a single L40S GPU and train for less than 2 hours. |

Hardware setup mentions:

- We train on H100 GPUs with bfloat16 mixed precision.

## Main results tables (from the paper)

**Table 1: Exact match accuracy (%) on Sudoku when sampling with 180 steps. The overall best is underlined. The best score with continuous diffusion is bolded. CANDI is a hybrid continuous-masked model, which can also be trained as a pure Gaussian diffusion model. $\mathbb{S}$-FLM is best, but similar to FLM.**

| Model | Easy | Med. | Hard |
| --- | --- | --- | --- |
| Autoregressive | Autoregressive | Autoregressive | Autoregressive |
| Sample | 13.9 | 5.1 | 0.6 |
| Greedy | 14.6 | 5.1 | 1.0 |
| Discrete Diffusion | Discrete Diffusion | Discrete Diffusion | Discrete Diffusion |
| MDLM (76) | 92.0 | 77.1 | 30.2 |
| Duo (77) | 96.3 | 84.7 | 58.4 |
| Continuous Diffusion | Continuous Diffusion | Continuous Diffusion | Continuous Diffusion |
| CANDI (69) | 79.3 | 45.9 | 16.7 |
| CANDI pure Gaussian | 63.9 | 41.9 | 12.5 |
| FLM (45) | 94.2 | 82.7 | 44.5 |
| $\mathbb{S}$-FLM | 81.5 | 50.6 | 14.0 |
| $\mathbb{S}$-FLM+ $\alpha^{\star}(0.1)$ | 94.0 | 77.6 | 43.2 |
| $\mathbb{S}$-FLM+ $\alpha^{\star}(0.1)$ + adaptive | 94.8 | 85.2 | 45.0 |

**Table 2: Accuracy (%) on GSM8K after 250k training steps. Diffusion variants use 1024 steps, no temperature scaling, AR uses 512 (context length). The overall best is underlined, the best continuous diffusion result is bolded. $\mathbb{S}$-FLM outperforms prior continuous diffusion language models, and closes the gap to MDLM at $T=1$ when the velocity (15) is computed with top-$k=1$. A gap remains**

| Model | Acc. (%) |
| --- | --- |
| Autoregressive | Autoregressive |
| Sample | 53.9 |
| Greedy | 63.3 |
| Discrete Diffusion | Discrete Diffusion |
| MDLM (76) | 18.0 |
| DUO (77) | 17.2 |
| Continuous Diffusion | Continuous Diffusion |
| CANDI (69) | 0.2 |
| FLM (45) | 0.3 |
| $\mathbb{S}$-FLM | 1.2 |
| + $\alpha^{\star}(0.1)$ | 7.7 |
| + no renorm. | 8.1 |
| + adaptive | 11.1 |
| + $\mathbb{S}$-arch. (Sec. 3.3) | 12.4 |
| + top-$k=1$ | 18.0 |

## Conclusion (from the paper)

We introduced \mathbb{S}-FLM, a Riemannian flow on \mathbb{S}^{d-1} that is competitive at language modeling with large vocabularies and learns the velocity and embeddings jointly. Beyond the formalism, our key contributions include the \mathbb{S}-arch backbone with normalized activations and the truncated and adaptive noise-schedule analysis. On Sudoku, \mathbb{S}-FLM performs similarly to prior FLMs. On GSM8K, where other FLMs fail, \mathbb{S}-FLM with top-1 decoding closes the gap to MDLM and Duo at T=1, though a gap remains at low-temperature (T=0.1) sampling. On OpenWebText, \mathbb{S}-FLM follows the Gen. PPL / Entropy frontier of prior FLMs at high NFE and beats them at low NFE where prior continuous models have an unstable frontier. \mathbb{S}-FLM avoids materializing |\mathcal{V}|-dimensional one-hot vectors, so it trains faster than prior FLMs and may be easier to scale (though this is speculation and left for future work). Our analysis of the sampling-dynamics provides a principled heuristic for truncating the noise schedule that works well in practice. At the same time, there remains a clear gap between diffusion models and AR models in GSM8K.

## Limitations (from the paper)

Continuous diffusion language models, including \mathbb{S}-FLM, underperform their discrete counterparts. In TinyGSM, \mathbb{S}-FLM reduces the gap compared to prior FLMs but does not eliminate it, and a substantial gap to autoregressive decoding remains (Sec. 4.2). The truncation threshold \alpha^{\star}(\delta) is derived a simplified model (Suppl. C.2) which assumes that the embeddings are randomly distributed on the sphere. The learned embeddings are likely more structured. Therefore, \alpha^{\star}(\delta) should only be treated as a principled heuristic for the truncation hyperparameter. A more sophisticated model of the sampling dynamics might improve the bound. The \mathbb{S}-arch follows the design of nGPT for simplicity and trains slower than the standard DiT. Improving its throughput is an important future direction. The training dynamics of \mathbb{S}-FLM should also be …

<details><summary>Paper outline</summary>

- 1 Introduction
- 2 Background
- 2.1 Flow Generative Modeling
- 2.2 Geometry of the Hypersphere
- 2.3 Flow Matching on the Hypersphere
- 3 Hyperspherical Flow Language Models
- 3.1 Sampling
- 3.2 Noise Schedule
- 3.3 Hyperspherical Architecture
- 4 Experiments
- 4.1 Reasoning on Sudoku
- 4.2 Reasoning on GSM8K
- 4.3 Language Modeling on OpenWebText
- 5 Related Work
- 6 Conclusion
- 7 Limitations
- 8 Impact Statement
- Acknowledgments and Disclosure of Funding
- Appendix A Background on Flow Matching
- A.1 The Continuity Equation
- A.2 Deriving the Conditional Velocity Fields
- Appendix B Additional Details
- B.1 Training and Sampling Pseudocode
- B.2 Adaptive Noise Schedule
- B.3 Hyperspherical Backbone Architecture
- B.4 Step Size with the Euler Sampler
- B.5 Re-Normalizing Embeddings
- B.6 Training Cost
- Appendix C Additional Results
- C.1 Sequence Length Using Different Tokenizers
- C.2 Analysis under the Random Codebook Model
- C.3 Sudoku Setup
- C.4 Additional Ablations on Sudoku
- C.5 Bootstrap Confidence Intervals on TinyGSM
- C.6 Additional Ablations on TinyGSM
- C.7 Additional Sampling Results on TinyGSM
- C.8 Gen. PPL / Entropy Frontier Protocol
- C.9 Additional Results on OpenWebText
- Appendix D Extended Related Work

</details>

## Reproduce / read

```sh
arxiv read 2605.11125            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2605.11125 "method"
arxiv bibtex 2605.11125
```
