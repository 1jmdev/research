# Register Tokens for Bounded-State Reasoning in Diffusion Language Models

**arXiv:** [2609.16372](https://arxiv.org/abs/2609.16372) · [PDF](https://arxiv.org/pdf/2609.16372) · [HTML](https://arxiv.org/html/2609.16372) · **First submitted:** 2026-09-14  
**Category:** [Diffusion language models (dLLMs) — models & training](README.md) — **rank #150 of 351** by impact score  

> **TL;DR** — Registers are implemented as dedicated fixed-position tokens whose continuous hidden states are trained to carry reasoning progress across generation chunks, which are especially effective for bounded code generation, where correct programs usually span several chunks.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Albert Ge, Chandan Singh, Yufan Zhuang, Xiaodong Liu, Jianfeng Gao, Frederic Sala |
| Organisation (HF) | sprocket-lab |
| arXiv categories | cs.CL |
| Citations (Semantic Scholar, 2026-09-25) | 0 (0 influential) |
| Hugging Face upvotes | 7 |
| Code | https://github.com/SprocketLab/dllm-registers |
| Models used | LLaDA, Dream-7, LLaDA-1.5 |
| Hardware | B200 (×6 mentions), A100 (×3 mentions), MI355X (×2 mentions), T4 (×1 mentions) |
| **Compute (est. H100-hours)** | **211 – 422** (auto-extracted, see below) |
| Impact score | 2.8 |

## Key results (claimed in abstract)

- In our main comparisons on LLaDA and Dream, registers outperform discrete-text carry on every benchmark, with gains of up to 8.5 points on math and 19.5 points on code.

## Abstract

Masked diffusion language models (dLLMs) generate text by iteratively denoising masked tokens with bidirectional attention. Extending reasoning across generation chunks normally requires keeping earlier generated text in context. We ask whether a dLLM can instead continue reasoning after that text is cleared, using only a fixed-size carried state. We implement this state as a small number of register tokens: dedicated fixed-position tokens whose continuous hidden states are trained to carry reasoning progress across generation chunks. We post-train dLLMs to decode a chunk of text, clear it while preserving the register values, and continue decoding from the prompt and carried state. In our main comparisons on LLaDA and Dream, registers outperform discrete-text carry on every benchmark, with gains of up to 8.5 points on math and 19.5 points on code. Registers are especially effective for bounded code generation, where correct programs usually span several chunks. Finally, registers can be further refined with reinforcement learning on long-horizon reasoning tasks.

## Contributions (from the paper)

Training registers is challenging because a new chunk can simply attend to the prompt or to its own unmasked tokens and predict the masked ones without using the registers. We therefore clear the generated text between chunks while preserving the register values, and during training we sometimes mask attention from the new chunk to the prompt and force some passes to predict the chunk from a fully masked state (see Fig. 2). When both are applied, the registers are the only link to the prompt and the preceding reasoning. Our contributions are: Learning to store reasoning state in registers: We train dLLMs to write continuous state into fixed register positions, clear the generated text, and reinsert the saved state into the next chunk. Our chunked SFT recipe supports two objectives: task-directed registers trained by the next-chunk loss, and memory tokens trained to reconstruct the preceding chunk. Comparing registers to alternatives: We compare registers with full-sequence SFT without carry, discrete-text carry, and reconstruction-trained memory tokens. Registers outperform discrete text in every main comparison and lead both carry baselines in 10 of 12, with their clearest advantage on code, where most successful register programs span several chunks. Register refinement via reinforcement learning: We integrate registers into chunked GRPO, which further improves the carried state on two multi-chunk tasks, Countdown and LongArithmetic. Code will be released at https://github.com/lbertge/dllm-registers-reasoning. The training mixture and the released checkpoints are availabl …

## Compute & hardware (extracted from full text)

Estimates are normalised to H100-hours with the factors in [METHODOLOGY.md](../../../METHODOLOGY.md). Always check the quoted sentence: the parser can't tell which experiment a number belongs to.

| Est. H100-h | Parsed | Source sentence |
| --- | --- | --- |
| 211 | 8× B200 × 12 h | Each checkpoint used 8 NVIDIA B200 GPUs for approximately 12 hours, or roughly 576 B200-GPU hours across the original SFT runs. |
| 422 | 8× B200 × 24 h | Each RL run used 8 NVIDIA B200 GPUs for approximately one day, for roughly 768 additional B200-GPU hours across the four reported RL runs. |

## Main results tables (from the paper)

**Table 1: Bounded generation performance, grouped by training setup. Both groups use the same bounded evaluation windows. Math uses first-answer accuracy at $C{=}128$ with up to 8 chunks; code uses pass@1 at $C{=}64$ with up to 16 chunks. All prompts are zero-shot and chat-formatted. Values are percentages with standard error. Bold marks the best chunk-trained method in each row; full-sequence SFT,**

|  | Full-sequence training | Chunked sequence training | Chunked sequence training | Chunked sequence training |
| --- | --- | --- | --- | --- |
| Benchmark | SFT | Discrete text | Memory tokens | Registers |
| LLaDA-8B-Base | LLaDA-8B-Base | LLaDA-8B-Base | LLaDA-8B-Base | LLaDA-8B-Base |
| Math ($C{=}128$, up to 8 chunks) | Math ($C{=}128$, up to 8 chunks) | Math ($C{=}128$, up to 8 chunks) | Math ($C{=}128$, up to 8 chunks) | Math ($C{=}128$, up to 8 chunks) |
| GSM8K | 57.4 $\pm$1.4 | 40.6 $\pm$1.4 | 48.4 $\pm$1.4 | 49.1 $\pm$1.4 |
| GSM-Hard | 21.7 $\pm$1.1 | 13.0 $\pm$0.9 | 18.6 $\pm$1.1 | 16.9 $\pm$1.0 |
| MATH500 | 22.4 $\pm$1.9 | 14.6 $\pm$1.6 | 15.6 $\pm$1.6 | 17.6 $\pm$1.7 |
| Omni-MATH easy | 23.6 $\pm$1.6 | 19.2 $\pm$1.5 | 20.9 $\pm$1.6 | 22.7 $\pm$1.6 |
| Code ($C{=}64$, up to 16 chunks) | Code ($C{=}64$, up to 16 chunks) | Code ($C{=}64$, up to 16 chunks) | Code ($C{=}64$, up to 16 chunks) | Code ($C{=}64$, up to 16 chunks) |
| HumanEval | 14.0 $\pm$2.7 | 18.3 $\pm$3.0 | 12.8 $\pm$2.6 | 26.2 $\pm$3.4 |
| MBPP | 25.7 $\pm$2.7 | 18.3 $\pm$2.4 | 10.5 $\pm$1.9 | 29.2 $\pm$2.8 |
| Dream-7B-Base | Dream-7B-Base | Dream-7B-Base | Dream-7B-Base | Dream-7B-Base |
| Math ($C{=}128$, up to 8 chunks) | Math ($C{=}128$, up to 8 chunks) | Math ($C{=}128$, up to 8 chunks) | Math ($C{=}128$, up to 8 chunks) | Math ($C{=}128$, up to 8 chunks) |
| GSM8K | 49.6 $\pm$1.4 | 35.0 $\pm$1.3 | 37.3 $\pm$1.3 | 42.9 $\pm$1.4 |
| GSM-Hard | 13.3 $\pm$0.9 | 10.5 $\pm$0.8 | 10.2 $\pm$0.8 | 12.1 $\pm$0.9 |
| MATH500 | 24.2 $\pm$1.9 | 10.8 $\pm$1.4 | 12.6 $\pm$1.5 | 12.4 $\pm$1.5 |
| Omni-MATH easy | 27.3 $\pm$1.7 | 16.4 $\pm$1.4 | 17.0 $\pm$1.4 | 19.3 $\pm$1.5 |
| … |

**Table 4: Intervening on the injected register state. Historical LLaDA GSM8K accuracy under the original paired-evaluation scorer. Noise is independently rescaled to each original slot norm, preserving scale while destroying direction.**

| State injected at each boundary | Accuracy |
| --- | --- |
| Intact carried registers | 46.4 |
| Norm-matched Gaussian noise | 32.7 |
| Initial register embeddings (reset) | 21.5 |

## Limitations (from the paper)

Registers do not replace full-context decoding: at a 1024-token horizon the full trace is more accurate (Sec. 4.3), and our timing study does not report peak memory, a visible-history blockwise decoder, or performance beyond the backbone context limit. Full-sequence SFT leads all eight C{=}128 math rows; the register gains over no-carry SFT are on code. At C{=}64, Discrete text overtakes SFT on GSM8K and GSM-Hard, while registers remain below it on every benchmark. These shorter-window controls evaluate carry models trained at C{=}128. Continuous and discrete slots are matched in count rather than information capacity, and their prompt-masking probabilities differ. We have not separately ablated masking or the number of denoising passes. Most SFT comparisons use one seed, and not every numerical lead is individually significant. Evaluation precision and runtime differences are …

<details><summary>Paper outline</summary>

- 1 Introduction
- 2 Related work
- 3 Methods: Register tokens for bounded-state reasoning
- 3.1 Background
- 3.2 Inference with register tokens
- 3.3 Training registers
- 4 Results: Bounded-state reasoning across generation chunks
- 4.1 Experimental Setup
- 4.2 Registers are the strongest carry mechanism overall
- 4.3 Full context is more accurate at short lengths; bounded carry scales better (historical checkpoints)
- 4.4 Registers support continued reasoning across chunks
- 4.5 Ablating the number of carry slots
- 4.6 Registers improve bounded-state reasoning with chunked diffu-GRPO
- 4.7 Interpreting learned registers
- 5 Discussion
- Acknowledgments and Disclosure of Funding
- Appendix A Chunked SFT pseudocode
- A.1 Why prompt masking and a fully masked chunk
- Appendix B Additional Experimental Details
- B.1 Additional bounded-control results
- B.2 Post-review controls and diagnostic evaluations
- Appendix C RL Details
- C.1 LongArithmetic Carryover vs. Reset
- Appendix D Correct-Trace Chunk Distributions
- D.1 Main-table runs
- D.2 Historical chunk distributions
- Appendix E Qualitative Carryover Examples
- Appendix F Linear Probe Details
- Appendix G Slot-Count Scaling Sweep
- G.1 Data scaling and schedule sensitivity at $N{=}16$
- Appendix H Synthetic Write/Read Training Study
- Appendix I Full Hyperparameter Table
- Appendix J Extended related work

</details>

## Reproduce / read

```sh
arxiv read 2609.16372            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2609.16372 "method"
arxiv bibtex 2609.16372
```
