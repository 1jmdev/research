# A JoLT for the KV cache: Near-Lossless KV Cache Compression via Joint Rank-bit Allocation

**arXiv:** [2607.12550](https://arxiv.org/abs/2607.12550) · [PDF](https://arxiv.org/pdf/2607.12550) · [HTML](https://arxiv.org/html/2607.12550) · **First submitted:** 2026-09-24  
**Category:** [KV cache eviction / token selection / sparse retrieval](README.md) — **rank #102 of 240** by impact score  
**Also relevant to:** [KV cache quantization](../quantization/README.md) · [Low-rank decomposition & weight factorization](../../compression/low-rank-decomposition/README.md) · [KV cache — general, systems & analysis](../_general/README.md) · [GPU kernels, compilers & operator optimization](../../serving-systems/gpu-kernels-and-compilers/README.md)  

> **TL;DR** — The method, JoLT (Joint Lagrangian Tucker), applies a partial Tucker decomposition that compresses only the token and feature axes while leaving the head and layer axes intact, then restores the energy that truncation discards with a rotated low-bit residual.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Rahul Krishnan, Volker Schulz |
| arXiv categories | cs.LG, cs.CL, math.OC |
| Venue | ICLR 2027 |
| arXiv comments | 9 pages, 5 figures, 16 tables. Under review at ICLR 2027 |
| Citations (Semantic Scholar, 2026-09-25) | 1 (0 influential) |
| Hugging Face upvotes | 0 |
| Code | — |
| Training requirement (from abstract) | training-free / post-training |
| Models used | LLaMA-3.1-8B |
| Impact score | 2.22 |

## Key results (claimed in abstract)

- Across five models from four architecture families, covering multi-head attention, grouped-query attention, and mixture-of-experts architecture, JoLT achieves 2 - 3x compression with less than 0.2% perplexity degradation, without retraining.
- On RULER at 64K context with LLaMA-3.1-8B, retrieval accuracy remains near-lossless through 3x and declines by only 0.90 and 2.40pp at 4x and 5x, respectively.

## Abstract

The key-value (KV) cache is the dominant memory bottleneck in long-context language model inference. Existing compression methods apply low-rank factorization or quantization independently, without jointly allocating rank and precision under a shared storage budget. We introduce JoLT, a training-free compressor that treats grouped prefill caches as fourth-order tensors and applies partial Tucker decomposition along the token and feature modes, the two axes that carry low-rank structure, while leaving the head and layer modes intact. A rotated low-bit quantizer captures the truncation residual, and a single Lagrangian dual allocates per-group Tucker ranks and residual bit-widths under a global byte constraint. FlashJoLT replaces the exact token-mode SVD with a randomized approximation that matches JoLT within the free zone at a fraction of the compression cost, and a fused Triton decode kernel evaluates attention directly over the stored factors without materializing dense KV tensors. Across five models from four architecture families, covering multi-head attention, grouped-query attention, and mixture-of-experts architecture, JoLT achieves 2 - 3x compression with less than 0.2% perplexity degradation, without retraining. On RULER at 64K context with LLaMA-3.1-8B, retrieval accuracy remains near-lossless through 3x and declines by only 0.90 and 2.40pp at 4x and 5x, respectively. JoLT demonstrates that tensor-aware low-rank decomposition and quantized residuals, unified under a single storage budget, achieve near-lossless KV-cache compression across diverse model architectures without retraining.

## Reproduce / read

```sh
arxiv read 2607.12550            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2607.12550 "method"
arxiv bibtex 2607.12550
```
