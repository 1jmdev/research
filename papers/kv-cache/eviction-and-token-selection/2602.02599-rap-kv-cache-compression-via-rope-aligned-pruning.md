# RAP: KV-Cache Compression via RoPE-Aligned Pruning

**arXiv:** [2602.02599](https://arxiv.org/abs/2602.02599) · [PDF](https://arxiv.org/pdf/2602.02599) · [HTML](https://arxiv.org/html/2602.02599) · **First submitted:** 2026-07-30  
**Category:** [KV cache eviction / token selection / sparse retrieval](README.md) — **rank #112 of 240** by impact score  
**Also relevant to:** [Long context, context extension & positional encoding](../../attention/long-context-and-position/README.md) · [KV cache — general, systems & analysis](../_general/README.md) · [Structured pruning (layers, heads, width, experts)](../../compression/structured-pruning/README.md)  

> **TL;DR** — This work proposes RoPE-Aligned Pruning (RAP), which constrains the pruning granularity to RoPE-aligned pairs rather than individual channels, and preserves accuracy at 30% KV compression, stays near the strongest low-rank method at lower attention cost, and composes with orthogonal methods such as quantization.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Jihao Xin, Tian Lyu, David Keyes, Hatem Ltaief, Marco Canini |
| arXiv categories | cs.LG, cs.AI |
| Citations (Semantic Scholar, 2026-09-25) | 2 (0 influential) |
| Hugging Face upvotes | 0 |
| Code | — |
| Models used | Llama, Mistral |
| Impact score | 1.78 |

## Key results (claimed in abstract)

- Our evaluation across Llama, Mistral, and Qwen models from 3B to 14B shows that RAP preserves accuracy at 30% KV compression (retain ratio \r{ho} = 0.7), far outperforms RoPE-blind channel pruning, stays near the strongest low-rank method at lower attention cost, and composes with orthogonal methods such as quantization.

## Abstract

Long-context inference in large language models (LLMs) is bottlenecked by the memory and compute of the key-value (KV) cache. Structured pruning is a direct way to shrink it: dropping the least useful channels of the W_k, W_v projection weights to reduce the output KV dimensions. However, modern LLMs apply Rotary Position Embedding (RoPE) after the QK projections, which rotates feature dimensions in pairs. Therefore, removing individual channels breaks these pairs, corrupting RoPE's positional semantics and rendering the pruned model unusable. We propose RoPE-Aligned Pruning (RAP), which constrains the pruning granularity to RoPE-aligned pairs rather than individual channels: removing whole pairs to keep the rotation intact. Our evaluation across Llama, Mistral, and Qwen models from 3B to 14B shows that RAP preserves accuracy at 30% KV compression (retain ratio \r{ho} = 0.7), far outperforms RoPE-blind channel pruning, stays near the strongest low-rank method at lower attention cost, and composes with orthogonal methods such as quantization.

## Reproduce / read

```sh
arxiv read 2602.02599            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2602.02599 "method"
arxiv bibtex 2602.02599
```
