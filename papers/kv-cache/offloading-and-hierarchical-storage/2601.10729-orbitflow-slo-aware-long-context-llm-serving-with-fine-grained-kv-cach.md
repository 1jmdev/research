# OrbitFlow: SLO-Aware Long-Context LLM Serving with Fine-Grained KV Cache Reconfiguration

**arXiv:** [2601.10729](https://arxiv.org/abs/2601.10729) · [PDF](https://arxiv.org/pdf/2601.10729) · [HTML](https://arxiv.org/html/2601.10729) · **First submitted:** 2026-03-02  
**Category:** [KV cache offloading & hierarchical storage](README.md) — **rank #14 of 62** by impact score  
**Also relevant to:** [Long context, context extension & positional encoding](../../attention/long-context-and-position/README.md) · [Request scheduling, batching & SLO serving](../../serving-systems/scheduling-and-batching/README.md) · [Memory management & offloading (weights, activations)](../../serving-systems/memory-and-offloading/README.md) · [KV cache — general, systems & analysis](../_general/README.md)  

> **TL;DR** — OrbitFlow is introduced, a finegrained and adaptive KV cache management system that meets latency SLOs in long-context LLM serving and improves SLO attainment for TPOT and TBT while reducing the 95th percentile latency by 38% and achieving up to 3.3× higher throughput compared to existing offloading methods.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Xinyue Ma, Heelim Hong, Taegeon Um, Jongseop Lee, Seoyeong Choy, Woo-Yeon Lee, Myeongjae Jeon |
| arXiv categories | cs.AI, cs.LG, cs.PF |
| Venue | VLDB 2026 |
| arXiv comments | Accepted at the 52nd International Conference on Very Large Data Bases (VLDB 2026). Xinyue Ma and Heelim Hong contributed equally (co-first authors) |
| Citations (Semantic Scholar, 2026-09-25) | 6 (0 influential) |
| Hugging Face upvotes | 0 |
| Code | — |
| Impact score | 2.65 |

## Key results (claimed in abstract)

- Our experiments demonstrate that OrbitFlow improves SLO attainment for TPOT and TBT by up to 66% and 48%, respectively, while reducing the 95th percentile latency by 38% and achieving up to 3.3x higher throughput compared to existing offloading methods.

## Abstract

Serving long-context LLMs is challenging because request lengths and batch composition vary during token generation, causing the memory footprint to fluctuate significantly at runtime. Offloading KV caches to host memory limits effective memory usage, but existing static and predetermined offloading strategies cannot adapt to the rapidly shifting memory demands of long-context serving. This often leads to excessive CPU-to-GPU KV transfers that translate into latency spikes and frequent SLO violations. To address these challenges, we introduce OrbitFlow, a fine-grained and adaptive KV cache management system that meets latency SLOs in long-context LLM serving. OrbitFlow employs a lightweight ILP solver to decide which layers' KV caches to retain on the GPU for each request, within memory capacity constraints. It continuously refines KV placements based on runtime feedback when the active plan becomes suboptimal during token generation. Under heavy load, OrbitFlow invokes a fallback mechanism to temporarily defer in-flight requests with large memory footprints, preserving overall SLO attainment. Our experiments demonstrate that OrbitFlow improves SLO attainment for TPOT and TBT by up to 66% and 48%, respectively, while reducing the 95th percentile latency by 38% and achieving up to 3.3x higher throughput compared to existing offloading methods.

## Reproduce / read

```sh
arxiv read 2601.10729            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2601.10729 "method"
arxiv bibtex 2601.10729
```
