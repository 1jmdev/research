# DualPath: Breaking the Storage Bandwidth Bottleneck in Agentic LLM Inference

**arXiv:** [2602.21548](https://arxiv.org/abs/2602.21548) · [PDF](https://arxiv.org/pdf/2602.21548) · [HTML](https://arxiv.org/html/2602.21548) · **First submitted:** 2026-02-26  
**Category:** [KV cache offloading & hierarchical storage](README.md) — **rank #2 of 62** by impact score  
**Also relevant to:** [Prefix caching & KV reuse (RAG, multi-turn, agents)](../prefix-caching-and-reuse/README.md) · [Request scheduling, batching & SLO serving](../../serving-systems/scheduling-and-batching/README.md) · [Prefill/decode disaggregation & phase-aware serving](../../serving-systems/disaggregated-prefill-decode/README.md) · [KV cache — general, systems & analysis](../_general/README.md)  

> **TL;DR** — DualPath is presented, an inference system that breaks this bottleneck by introducing dual-path KV-Cache loading and enables a novel storage-to-decode path, in which the KV-Cache is loaded into decoding engines and then efficiently transferred to prefill engines via RDMA over the compute network.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Yongtong Wu, Shaoyuan Chen, Yinmin Zhong, Rilin Huang, Yixuan Tan, Wentao Zhang, Liyue Zhang, Shangyan Zhou, Yuxuan Liu, Shunfeng Zhou, … (+3) |
| Organisation (HF) | DeepSeek |
| arXiv categories | cs.DC |
| Citations (Semantic Scholar, 2026-09-25) | 19 (2 influential) |
| Hugging Face upvotes | 55 |
| Code | — |
| Impact score | 9.25 |

## Key results (claimed in abstract)

- Our evaluation on three models with production agentic workloads demonstrates that DualPath improves offline inference throughput by up to 1.87$\times$ on our in-house inference system.
- It can also improve online serving throughput by an average factor of 1.96$\times$ without violating SLO.

## Abstract

The performance of multi-turn, agentic LLM inference is increasingly dominated by KV-Cache storage I/O rather than computation. In prevalent disaggregated architectures, loading the massive KV-Cache from external storage creates a fundamental imbalance: storage NICs on prefill engines become bandwidth-saturated, while those on decoding engines remain idle. This asymmetry severely constrains overall system throughput. We present DualPath, an inference system that breaks this bottleneck by introducing dual-path KV-Cache loading. Beyond the traditional storage-to-prefill path, DualPath enables a novel storage-to-decode path, in which the KV-Cache is loaded into decoding engines and then efficiently transferred to prefill engines via RDMA over the compute network. DualPath combines this optimized data path -- which inherently avoids network congestion and avoids interference with latency-critical model execution communications -- with a global scheduler that dynamically balances load across prefill and decode engines. Our evaluation on three models with production agentic workloads demonstrates that DualPath improves offline inference throughput by up to 1.87$\times$ on our in-house inference system. It can also improve online serving throughput by an average factor of 1.96$\times$ without violating SLO.

**Keywords:** KV-Cache, prefill engines, decoding engines, RDMA, global scheduler, offline inference, online serving

## Reproduce / read

```sh
arxiv read 2602.21548            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2602.21548 "method"
arxiv bibtex 2602.21548
```
