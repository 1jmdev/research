# KV Packet: Recomputation-Free Context-Independent KV Caching for LLMs

**arXiv:** [2604.13226](https://arxiv.org/abs/2604.13226) · [PDF](https://arxiv.org/pdf/2604.13226) · [HTML](https://arxiv.org/html/2604.13226) · **First submitted:** 2026-04-17  
**Category:** [Prefix caching & KV reuse (RAG, multi-turn, agents)](README.md) — **rank #6 of 106** by impact score  

> **TL;DR** — KV Packet is proposed, a recomputation-free cache reuse framework that treats cached documents as immutable ``packets''wrapped in light-weight trainable soft-token adapters, which are trained via self-supervised distillation to bridge context discontinuities.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Chuangtao Chen, Grace Li Zhang, Xunzhao Yin, Cheng Zhuo, Bing Li, Ulf Schlichtmann |
| Organisation (HF) | Technical University of Munich |
| arXiv categories | cs.LG, cs.AI |
| Citations (Semantic Scholar, 2026-09-25) | 5 (1 influential) |
| Hugging Face upvotes | 10 |
| Code | https://github.com/ChuangtaoChen-TUM/KVPacket |
| Training requirement (from abstract) | involves (re)training / fine-tuning |
| Models used | Llama-3.1-8B, Llama, Llama-3.1, Qwen2.5, Llama-3, Qwen-3, Qwen-3-4B |
| Hardware | A100 (×1 mentions) |
| Impact score | 5.98 |

## Abstract

Large Language Models (LLMs) rely heavily on Key-Value (KV) caching to minimize inference latency. However, standard KV caches are context-dependent: reusing a cached document in a new context requires recomputing KV states to account for shifts in attention distribution. Existing solutions such as CacheBlend, EPIC, and SAM-KV mitigate this issue by selectively recomputing a subset of tokens; however, they still incur non-negligible computational overhead (FLOPs) and increased Time-to-First-Token (TTFT) latency. In this paper, we propose KV Packet, a recomputation-free cache reuse framework that treats cached documents as immutable ``packets'' wrapped in light-weight trainable soft-token adapters, which are trained via self-supervised distillation to bridge context discontinuities. Experiments on Llama-3.1 and Qwen2.5 demonstrate that the proposed KV Packet method achieves near-zero FLOPs and lower TTFT than recomputation-based baselines, while retaining F1 scores comparable to those of the full recomputation baseline.

## Contributions (from the paper)

The key contributions of this paper are as follows: Zero-Recomputation Architecture: We introduce the KV Packet abstraction, which wraps frozen document caches with trainable soft-token adapters. This design eliminates the need for model parameter modifications and expensive cache recomputation during inference. Self-Supervised Alignment: We propose a self-distillation training objective for the trainable fillers that aligns disjointed KV packets with full-attention representations. This approach requires no human-labeled data and restores contextual connectivity without modifying the base model’s parameters. Efficiency and Performance: We demonstrate that KV Packet reduces computational overhead (FLOPs) by approximately 4 orders of magnitude compared to state-of-the-art methods like CacheBlend and EPIC, while maintaining competitive performance on various tasks. Compatibility with KV Compression: As a recomputation-free architecture, KV Packet natively supports existing KV compression techniques seamlessly, while recomputation-based methods struggle with unstructured compression with varying token preservation across layers. The remainder of this paper is organized as follows: Section 2 reviews KV caching mechanics and related work. Section 3 details the KV Packet architecture and self-supervised distillation objective. Section 4 evaluates performance, latency, and computational efficiency against state-of-the-art baselines, including compatibility experiments and ablation studies. Section 5 summarizes our findings and discusses limitations and future directions.

## Compute & hardware (extracted from full text)

Hardware setup mentions:

- All experiments use a single NVIDIA A100 (80 GB) GPU with 8 adapter tokens (N_{h}=N_{t}=8), float32 adapters, and models in bfloat16.

## Main results tables (from the paper)

**Table 1: Cross-domain generalization analysis. Rows represent the dataset used to train the KV Packet adapters for Llama-3.1-8B-Instruct, while columns indicate the evaluation performance (F1 score) across different domains.**

|  | Evaluation Dataset | Evaluation Dataset | Evaluation Dataset | Evaluation Dataset |
| --- | --- | --- | --- | --- |
| Training Dataset | Biography | HotpotQA | MusiQue | NIAH |
| No Recompute | 0.06 | 0.24 | 0.01 | 0.39 |
| Biography | 0.96 | 0.18 | 0.16 | 0.44 |
| HotpotQA | 0.88 | 0.44 | 0.39 | 0.36 |
| MusiQue | 0.87 | 0.36 | 0.37 | 0.34 |
| Needle-in-a-Haystack (NIAH) | 0.54 | 0.28 | 0.07 | 0.80 |
| Universal (Mixture) | 0.95 | 0.42 | 0.43 | 0.71 |

## Conclusion (from the paper)

We presented KV Packet, a recomputation-free framework for context-independent KV caching. By wrapping immutable document caches with lightweight trainable Headers and Trailers, we address boundary artifacts that arise from naive cache concatenation, as validated by attention score analysis. Our self-supervised distillation objective trains these adapters without human-labeled data, keeping base model weights frozen. KV Packet integrates seamlessly with existing KV compression techniques, a compatibility that recomputation-based methods fundamentally cannot match, and reduces inference-time FLOPs by 5–6 orders of magnitude while matching state-of-the-art accuracy on retrieval and reasoning benchmarks. Limitations and Future Work Adapter effectiveness assumes the retrieval corpus is reasonably aligned with the training distribution; generalization to highly out-of-distribution domains remains an open question. Current evaluations cover only a limited set of model families due to the engineering complexity of validating recomputation baselines for each architecture. KV Packet targets settings where retrieved documents are largely independent; behavior under dependent document chains such as multi-step reasoning traces warrants further investigation.

<details><summary>Paper outline</summary>

- 1 Introduction
- 2 Background and Related Work
- 2.1 Key-Value Caching and Positional Encoding
- 2.2 Context-Independent KV Caching
- 2.3 Soft-token Adapter Methods
- 3 Methods
- 3.1 Formulation of KV Packet
- 3.2 Training via Self-Supervised Distillation
- 4 Experiments
- 4.1 Experimental Setup
- 4.2 Efficiency Metrics
- 4.3 Results and Analysis
- 4.4 Compatibility with KV Compression
- 4.5 Ablation Study
- 5 Conclusion

</details>

**Keywords:** Key-Value caching, attention distribution, CacheBlend, EPIC, SAM-KV, self-supervised distillation, soft-token adapters, context discontinuities, FLOPs, Time-to-First-Token, Llama-3.1, Qwen2.5

## Reproduce / read

```sh
arxiv read 2604.13226            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2604.13226 "method"
arxiv bibtex 2604.13226
```
