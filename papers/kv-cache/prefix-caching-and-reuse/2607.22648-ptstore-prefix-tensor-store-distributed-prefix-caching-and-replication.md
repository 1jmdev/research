# PTStore (Prefix Tensor Store): Distributed Prefix Caching and Replication for High Throughput Inference Serving

**arXiv:** [2607.22648](https://arxiv.org/abs/2607.22648) · [PDF](https://arxiv.org/pdf/2607.22648) · [HTML](https://arxiv.org/html/2607.22648) · **First submitted:** 2026-06-25  
**Category:** [Prefix caching & KV reuse (RAG, multi-turn, agents)](README.md) — **rank #73 of 106** by impact score  
**Also relevant to:** [Request scheduling, batching & SLO serving](../../serving-systems/scheduling-and-batching/README.md) · [KV cache — general, systems & analysis](../_general/README.md) · [Serving & inference systems — general](../../serving-systems/_general/README.md)  

> **TL;DR** — PTStore distributes and replicates popular tensors that form reusable KV cache prefixes, which reduces the latency of accessing the KV cache and alleviates load imbalance caused by a disproportionately large number of requests on servers containing popular tensors.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Meghana Maghyastha, Robert Underwood, Randal Burns, Bogdan Nicolae |
| arXiv categories | cs.AI, cs.DC |
| Venue | European Conference on Parallel Processing (Eur Conf Parallel Process) |
| Citations (Semantic Scholar, 2026-09-25) | 1 (0 influential) |
| Hugging Face upvotes | 0 |
| Code | — |
| Impact score | 0.8 |

## Key results (claimed in abstract)

- As a result, PTStore can execute inferences on long passage Q\&A datasets 5-6 times more efficiently than current baselines, which do not aggregate memory across different nodes and GPUs and therefore require regenerating the KV cache.

## Abstract

Inspired by the design of client caching in Content Delivery Networks (CDNs), PTStore distributes and replicates popular tensors that form reusable KV cache prefixes, which are the main technique used by state of art approaches to accelerate inferences. This reduces the latency of accessing the KV cache and alleviates load imbalance caused by a disproportionately large number of requests on servers containing popular tensors. Furthermore, thanks to decentralization, PTStore allows the expansion of the size of the KV cache for LLM inference by orders of magnitude. As a result, PTStore can execute inferences on long passage Q\&A datasets 5-6 times more efficiently than current baselines, which do not aggregate memory across different nodes and GPUs and therefore require regenerating the KV cache.

## Reproduce / read

```sh
arxiv read 2607.22648            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2607.22648 "method"
arxiv bibtex 2607.22648
```
