# TokenDance: Scaling Multi-Agent LLM Serving via Collective KV Cache Sharing

**arXiv:** [2604.03143](https://arxiv.org/abs/2604.03143) · [PDF](https://arxiv.org/pdf/2604.03143) · [HTML](https://arxiv.org/html/2604.03143) · **First submitted:** 2026-04-03  
**Category:** [Prefix caching & KV reuse (RAG, multi-turn, agents)](README.md) — **rank #30 of 106** by impact score  
**Also relevant to:** [KV cache cross-layer sharing & merging](../cross-layer-sharing/README.md) · [Request scheduling, batching & SLO serving](../../serving-systems/scheduling-and-batching/README.md) · [KV cache — general, systems & analysis](../_general/README.md)  

> **TL;DR** — TokenDance is presented, a system that scales the number of concurrent agents by exploiting the All-Gather pattern for collective KV Cache sharing and performs KV Cache reuse over the full round in one collective step, so the cost of reusing a shared block is paid once regardless of agent count.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Zhuohang Bian, Feiyang Wu, Chengrui Zhang, Hangcheng Dong, Yun Liang, Youwei Zhuo |
| arXiv categories | cs.DC |
| arXiv comments | 14 pages, 14 figures, arXiv:submit/7438760 [cs.DC], preprint under review |
| Citations (Semantic Scholar, 2026-09-25) | 6 (0 influential) |
| Hugging Face upvotes | 0 |
| Code | — |
| Impact score | 2.11 |

## Key results (claimed in abstract)

- Its Diff-Aware Storage encodes sibling caches as block-sparse diffs against a single master copy, achieving 11-17x compression on representative workloads.
- Evaluation on GenerativeAgents and AgentSociety shows that TokenDance supports up to 2.7x more concurrent agents than vLLM with prefix caching under SLO requirement, reduces per-agent KV Cache storage by up to 17.5x, and achieves up to 1.9x prefill speedup over per-request position-independent caching.

## Abstract

Multi-agent LLM applications organize execution in synchronized rounds where a central scheduler gathers outputs from all agents and redistributes the combined context. This All-Gather communication pattern creates massive KV Cache redundancy, because every agent's prompt contains the same shared output blocks, yet existing reuse methods fail to exploit it efficiently. We present TokenDance, a system that scales the number of concurrent agents by exploiting the All-Gather pattern for collective KV Cache sharing. TokenDance's KV Collector performs KV Cache reuse over the full round in one collective step, so the cost of reusing a shared block is paid once regardless of agent count. Its Diff-Aware Storage encodes sibling caches as block-sparse diffs against a single master copy, achieving 11-17x compression on representative workloads. Evaluation on GenerativeAgents and AgentSociety shows that TokenDance supports up to 2.7x more concurrent agents than vLLM with prefix caching under SLO requirement, reduces per-agent KV Cache storage by up to 17.5x, and achieves up to 1.9x prefill speedup over per-request position-independent caching.

## Reproduce / read

```sh
arxiv read 2604.03143            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2604.03143 "method"
arxiv bibtex 2604.03143
```
