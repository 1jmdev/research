# CommVQ: Commutative Vector Quantization for KV Cache Compression

**arXiv:** [2506.18879](https://arxiv.org/abs/2506.18879) · [PDF](https://arxiv.org/pdf/2506.18879) · [HTML](https://arxiv.org/html/2506.18879) · **First submitted:** 2025-06-23  
**Category:** [KV cache quantization](README.md) — **rank #2 of 73** by impact score  
**Also relevant to:** [KV cache eviction / token selection / sparse retrieval](../eviction-and-token-selection/README.md) · [Long context, context extension & positional encoding](../../attention/long-context-and-position/README.md) · [KV cache — general, systems & analysis](../_general/README.md)  

> **TL;DR** — This work introduces additive quantization with a lightweight encoder and codebook to compress the KV cache, and designs the codebook to be commutative with Rotary Position Embedding and train it using an Expectation-Maximization algorithm to enable efficient integration of decoding into the self-attention mechanism.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Junyan Li, Yang Zhang, Muhammad Yusuf Hassan, Talha Chafekar, Tianle Cai, Zhile Ren, Pengsheng Guo, Foroozan Karimzadeh, Colorado Reed, Chong Wang, … (+1) |
| arXiv categories | cs.CL, cs.AI |
| Venue | ICML 2025 |
| arXiv comments | ICML 2025 poster |
| Citations (Semantic Scholar, 2026-09-25) | 27 (3 influential) |
| Hugging Face upvotes | 5 |
| Code | https://github.com/UMass-Embodied-AGI/CommVQ |
| Bit-widths mentioned | 1, 2-bit |
| Models used | LLaMA-3.1-8B, LLaMA, LLaMA-3.1, LLaMA-2-8B, Mistral-8B, LLaMA-2-7B, Mistral-7B, LLaMA-2 |
| Hardware | H100 (×2 mentions), RTX 4090 (×2 mentions) |
| Impact score | 8.03 |

## Key results (claimed in abstract)

- Experiments on long-context benchmarks and GSM8K show that our method reduces FP16 KV cache size by 87.5% with 2-bit quantization, while outperforming state-of-the-art KV cache quantization methods.
- Notably, it enables 1-bit KV cache quantization with minimal accuracy loss, allowing a LLaMA-3.1 8B model to run with a 128K context length on a single RTX 4090 GPU.

## Abstract

Large Language Models (LLMs) are increasingly used in applications requiring long context lengths, but the key-value (KV) cache often becomes a memory bottleneck on GPUs as context grows. To address this, we propose Commutative Vector Quantization (CommVQ) to significantly reduce memory usage for long-context LLM inference. We first introduce additive quantization with a lightweight encoder and codebook to compress the KV cache, which can be decoded via simple matrix multiplication. To further reduce computational costs during decoding, we design the codebook to be commutative with Rotary Position Embedding (RoPE) and train it using an Expectation-Maximization (EM) algorithm. This enables efficient integration of decoding into the self-attention mechanism. Our approach achieves high accuracy with additive quantization and low overhead via the RoPE-commutative codebook. Experiments on long-context benchmarks and GSM8K show that our method reduces FP16 KV cache size by 87.5% with 2-bit quantization, while outperforming state-of-the-art KV cache quantization methods. Notably, it enables 1-bit KV cache quantization with minimal accuracy loss, allowing a LLaMA-3.1 8B model to run with a 128K context length on a single RTX 4090 GPU. The source code is available at: https://github.com/UMass-Embodied-AGI/CommVQ.

## Contributions (from the paper)

By combining these innovations, CommVQ achieves a superior trade-off between memory savings and accuracy. Extensive evaluation on two long-context benchmarks, LongBench (Bai et al., 2023) and InfiniteBench (Zhang et al., 2024b), as well as GSM8K (Cobbe et al., 2021), a benchmark designed for complex reasoning, shows that compared to state-of-the-art KV cache quantization baselines, we achieve nearly lossless KV cache compression with 2-bit quantization, outperforming other methods. Furthermore, we achieve 1-bit quantization with significantly better accuracy than existing baselines, demonstrating the effectiveness of our method in pushing the compression limits of KV cache. We summarize our contributions as follows: Unlike prior work that quantizes each scalar individually in the KV cache, we quantize each vector as a whole in the KV cache into a low bit-width representation using a learned codebook. By leveraging the commutative property of the RoPE matrix and the characteristics of self-attention, we refine our codebook to be RoPE-commutative. This refinement enables us to reformulate the self-attention computation to incorporate the decoding process more efficiently. Additionally, we provide a Triton implementation of our method to demonstrate real memory savings. Extensive experiments demonstrate the superiority of our method, particularly in ultra-low-bitwidth KV cache quantization scenarios (e.g., 1-bit quantization). This opens up the possibility of serving long-context LLMs under limited GPU memory constraints.

## Compute & hardware (extracted from full text)

Hardware setup mentions:

- Notably, it enables 1-bit KV cache quantization with minimal accuracy loss, allowing a LLaMA-3.1 8B model to run with a 128K context length on a single RTX 4090 GPU.
- This makes it impossible to run inference on a single GPU without KV cache offloading, even for H100-80GB.

## Main results tables (from the paper)

**Table 5: Latency comparison between the naive implementation and the optimized implementation utilizing the commutative codebook. Latency per layer per token is measured for context lengths of 8K, 32K, and 128K, reported in milliseconds (ms).**

| Latency (ms) | 8K | 32K | 128K |
| --- | --- | --- | --- |
| Naive Impl. | 2.4 | 9.2 | 36.6 |
| Optimized Impl. | 0.4 | 1.1 | 3.8 |
| Speedup | 6.0 | 8.4 | 9.6 |

**Table 6: Perplexity (PPL) comparison between FP16 baseline and CommVQ-2 across different domains.**

| Method | FineWeb-Edu | GSM-8K | Repobench-p | KV_Retrieval |
| --- | --- | --- | --- | --- |
| FP16 | 10.17 | 5.67 | 2.20 | 31.93 |
| CommVQ-2 | 11.54 | 6.14 | 2.78 | 32.72 |
| PPL Diff | +1.37 | +0.47 | +0.58 | +0.79 |

## Conclusion (from the paper)

We introduce CommVQ, a novel KV cache quantization approach for long-context LLMs. By leveraging vector quantization and a RoPE-commutative codebook, CommVQ significantly reduces KV cache size while maintaining high computational efficiency. Evaluations on long-context benchmarks show that CommVQ outperforms existing KV cache quantization methods, enabling more memory-efficient and scalable long-context LLM inference.

<details><summary>Paper outline</summary>

- 1 Introduction
- 2 Related Works
- 3 Preliminaries
- 3.1 Self-Attention and KV Cache
- 3.2 Rotary Position Embedding
- 4 Method
- 4.1 Learning Additive Quantization for KV Cache
- 4.2 Commutative Codebook for Efficiency
- 5 Experiments
- 5.1 Settings
- 5.2 Long Context Benchmarks Evaluation
- 5.3 GSM8K Evaluation
- 5.4 Model Ablation
- 5.5 Robustness Analysis Under Domain Shift
- 5.6 Efficiency Results
- 6 Conclusion
- Impact Statement
- Appendix A Appendix
- A.1 Explanation of Encoding and Decoding Using Commutative Codebooks
- A.2 EM Algorithm Implementation Details
- A.3 Codebook Size Analysis
- A.4 Ablation Study on Commutative Codebook Configuration
- A.5 Quantization Error Comparison

</details>

**Keywords:** Commutative Vector Quantization, CommVQ, additive quantization, codebook, Rotary Position Embedding, RoPE, Expectation-Maximization, self-attention, FP16, KV cache quantization, GSM8K, LLaMA-3.1 8B model

## Reproduce / read

```sh
arxiv read 2506.18879            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2506.18879 "method"
arxiv bibtex 2506.18879
```
