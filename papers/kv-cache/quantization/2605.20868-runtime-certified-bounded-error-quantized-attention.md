# Runtime-Certified Bounded-Error Quantized Attention

**arXiv:** [2605.20868](https://arxiv.org/abs/2605.20868) · [PDF](https://arxiv.org/pdf/2605.20868) · [HTML](https://arxiv.org/html/2605.20868) · **First submitted:** 2026-05-20  
**Category:** [KV cache quantization](README.md) — **rank #27 of 73** by impact score  
**Also relevant to:** [4-bit integer quantization (INT4, W4A16, W4A8, W4A4)](../../quantization/4-bit-integer/README.md) · [KV cache — general, systems & analysis](../_general/README.md)  

> **TL;DR** — A tiered KV cache architecture that enables runtime-certified attention: INT8 keys and INT4 values are stored in GPU memory, while FP16 originals are retained in system RAM for deterministic fallback and adaptive precision selection and a multi-stage fallback ladder.

## At a glance

| Field | Value |
| --- | --- |
| Authors | Dean Calver |
| arXiv categories | cs.LG, cs.AI, cs.SY, eess.SY |
| arXiv comments | 32 pages, 1 figure |
| Citations (Semantic Scholar, 2026-09-25) | 3 (1 influential) |
| Hugging Face upvotes | 0 |
| Code | https://github.com/DeanoC/certified-quantized-attention |
| Bit-widths mentioned | 4, 8-bit |
| Models used | LLaMA-3.1-8B, LLaMA, Falcon |
| Hardware | B200 (×3 mentions), RTX PRO 6000 (×2 mentions) |
| Impact score | 2.74 |

## Key results (claimed in abstract)

- We present a tiered KV cache architecture that enables runtime-certified attention: INT8 keys and INT4 values are stored in GPU memory, while FP16 originals are retained in system RAM for deterministic fallback.

## Abstract

KV cache quantization reduces the memory cost of long-context LLM inference, but introduces approximation error that is typically validated only empirically. Existing systems rely on average-case robustness, with no mechanism to detect or recover from failures at runtime. We present a tiered KV cache architecture that enables runtime-certified attention: INT8 keys and INT4 values are stored in GPU memory, while FP16 originals are retained in system RAM for deterministic fallback. A two-term error decomposition yields per-head, per-step bounds on (i) attention distribution distortion from key quantization and (ii) value reconstruction error. These bounds are computed online and used to drive adaptive precision selection and a multi-stage fallback ladder, which guarantees recovery to the exact dense attention output when required. Across PG-19, NIAH, and RULER benchmarks on LLaMA~3.1-8B with contexts up to 128K, the system matches dense FP16 KV quality within noise for language modelling and retrieval tasks, while recovering catastrophic failures observed in naive INT8/INT4 baselines. Value-sensitive tasks at short context expose a controlled trade-off between compression and fidelity, which can be eliminated via tighter value tolerances or FP16-value fallback. The certification is local (per-head, per-step) and does not guarantee end-to-end model correctness, but ensures that each attention computation is either bounded relative to an FP16 reference or exactly recovered via fallback. This reframes KV cache quantization as a runtime-verified computation rather than a fixed approximation. The goal is not raw speedups, but enabling safe deployment of aggressive KV compression under strict quality constraints.

## Contributions (from the paper)

Recent work pushes compression further. TurboQuant [24] provides theoretically grounded online vector quantization applicable to KV caches. JanusQuant [25] demonstrates accurate 2-bit KV cache quantization for long-context inference. PackInfer [26] targets compute/IO-efficient attention kernels for packed quantized caches in batched serving. These optimise compression ratio and throughput; our contribution is orthogonal: runtime certification and exact fallback rather than more aggressive compression.

## Compute & hardware (extracted from full text)

Hardware setup mentions:

- All experiments use a single NVIDIA RTX PRO 6000 WS (96 GB VRAM, Blackwell architecture) with pinned system RAM for Tier 2 storage.
- All performance measurements use the native Blackwell attention backend with the same configuration as the quality runs (Section 8), measured on a single RTX PRO 6000 WS with an AMD EPYC 9534 64-core CPU and 221 GB system RAM over PCIe 5.0 \times16.

## Main results tables (from the paper)

**Table 3: Headline quality deltas: certified quantized attention vs. dense FP16 attention (LLaMA 3.1-8B). $\Delta$ = Certified $-$ Dense; positive means certified is better. All results use asymmetric INT8 keys, INT4 values ($g{=}16$), FP32 accumulators, $k_{\max}{=}128$, $\tau_{\mathrm{cov}}{=}0.995$, $v_{\mathrm{tol}}{=}0.05$.**

| Benchmark | Metric | 8K | 32K | 64K | 128K | $n$ |
| --- | --- | --- | --- | --- | --- | --- |
| PG-19 | $\Delta$ ppl | $-0.002$ | $+0.001$ | $+0.001$ | $+0.001$ | 20 chunks |
| NIAH | $\Delta$ accuracy | $0\%$ | $-2\%$ | $0\%$ | —$\dagger$ | 100 paired trials |
| RULER | $\Delta$ accuracy | $-0.069$ | $-0.019$ | $+0.015$ | $+0.004$ | 20 slices |

**Table 4: Three-way quality comparison: Dense FP16 KV, Naive INT8K/INT4V (no certification), and Certified INT8K/INT4V (with adaptive promotion and fallback). NIAH accuracy is the fraction of needles retrieved (10 per trial, 100 paired trials). RULER accuracy is the subtask-averaged score; naive uses 10 slices (70 trials), certified uses 20 slices (140 trials)—the two are *not* paired on the same s**

| Benchmark | Ctx | Dense | Naive | Certified |
| --- | --- | --- | --- | --- |
| NIAH (acc) | 8K | 39% | 8% | 39% |
|  | 32K | 51% | 10% | 49% |
|  | 64K | 66% | 5% | 66% |
| RULER (acc) | 8K | 0.923 | 0.361 | 0.854 |
|  | 32K | 0.882 | 0.438 | 0.863 |
|  | 64K | 0.906 | 0.429 | 0.921 |
|  | 128K | 0.782 | 0.312 | 0.786 |
| Latency (ms) | 64K | 63.2 | 244.3 | 260.0 |
|  | 128K | 73.7 | 468.1 | 342.6 |

## Conclusion (from the paper)

We presented a tiered KV cache architecture that stores INT8 keys and INT4 values in GPU memory (Tier-1: 56% of FP16; runtime VRAM includes an additional FP16 key scratch buffer sized by the GQA working-set union) while retaining full-precision originals in system RAM (Tier-2) for runtime fallback. A two-term error decomposition provides independent, per-head, per-step bounds on key compression error and value compression error, each with its own runtime monitor and escalation path. An adaptive top-K precision selector promotes high-mass blocks to FP16 keys based on the actual attention pattern at each decode step, and a four-rung fallback ladder terminates in the Dense baseline’s own torch.scaled_dot_product_attention code path with unmodified FP16 KV. The certification is local (per-head, per-step) and does not imply end-to-end task-correctness guarantees. The system introduces a ranking-consistency check that detects when INT8 scoring noise distorts the attention distribution and triggers per-head fallback to dense attention. This mechanism is critical: it converts a previously unaddressed silent failure mode into a detectable and recoverable event. On retrieval-adversarial benchmarks, non-uniform precision across the sequence can otherwise cause attention dilution that degrades accuracy even when per-step error bounds are satisfied. On language modelling (PG-19), the …

## Limitations (from the paper)

The certified guarantee is per-head, per-step: it bounds \lVert O_{\mathrm{quant}}-O_{\mathrm{ref}}\rVert (Section 4.1), not the distance to O_{\mathrm{dense}}. It does not provide an end-to-end model-quality guarantee. On NIAH at 64K (100 paired trials), dense and certified achieve identical 66% accuracy with no statistically significant difference (exact McNemar p{=}1.0), but the NIAH precision ablation (Table 23) demonstrates that individual precision components (INT4 values, INT8 tail keys, kernel numerical path) each contribute measurable variance at the per-trial level. The aggregate effect cancels to noise at the operating point, but could become significant on tasks with sharper retrieval requirements or longer autoregressive chains. The aggregate effect on model quality must therefore be assessed empirically per deployment. The system RAM requirement for Tier 2 (FP16 originals) …

<details><summary>Paper outline</summary>

- 1 Introduction
- 1.1 Contributions
- 2 Background and Notation
- 2.1 Attention in Autoregressive Decoding
- 2.2 Block Organisation
- 2.3 Quantization Schemes
- 3 System Architecture
- 3.1 Tiered KV Cache
- 3.2 Fused Quantized Attention Kernel
- 3.3 Adaptive Precision Selection
- 3.4 Fallback Ladder
- 4 Formal Error Bounds
- 4.1 Two-Term Error Decomposition
- 4.2 Theorem 1: Value Compression Error
- 4.3 Theorem 2: Key Compression Error
- 4.4 INT8 Score Error for Mass Estimation
- 4.5 Total Error Bound
- 4.6 Preconditions and Monitoring
- 5 Compressed-Domain Execution
- 5.1 Bandwidth Analysis
- 5.2 Per-Channel Key Scoring
- 6 Instability Detection
- 6.1 Ranking-Consistency Check
- 7 Related Work
- 7.1 KV Cache Compression
- 7.2 Compressed-Domain Execution
- 7.3 Adaptive Precision
- 7.4 Certified Bounds in Attention
- 7.5 KV Cache Memory Management
- 7.6 Positioning Summary
- 8 Experimental Setup
- 9 Results
- 9.1 Summary
- 9.2 Naive Quantized Baseline
- 9.3 PG-19 Perplexity
- 9.4 RULER
- 9.5 Needle-in-a-Haystack (NIAH)
- 9.6 Certificate Telemetry
- 9.7 Storage Analysis
- 9.8 Quality Delta Summary
- 9.9 Runtime Performance
- 9.10 Numerical Precision of the Attention Kernel
- 10 Discussion
- 11 Limitations
- 12 Conclusion
- Artifact Availability
- Appendix A Total Variation Bound for Perturbed Softmax
- Appendix B Development Diagnostics: NIAH Precision Ablation

</details>

## Reproduce / read

```sh
arxiv read 2605.20868            # full paper as Markdown (github.com/1jmdev/arxiv)
arxiv section 2605.20868 "method"
arxiv bibtex 2605.20868
```
